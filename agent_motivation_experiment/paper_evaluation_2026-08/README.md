# 논문 evaluation 절 — 어떤 실험을, 무엇을 물으려고, 어느 데이터로

**2026-08-21에 만들었다.** 이 폴더는 evaluation 절 하나에 대해 **"무엇을 묻는가 / 어느 run이
답하는가 / 어느 그림이 그것을 보이는가 / 지금 어디까지 됐는가"**를 한 곳에 모은다.

## 이 폴더가 다른 세 곳과 어떻게 나뉘는가

| 어디 | 무엇 |
|---|---|
| **여기** (`paper_evaluation_2026-08/`) | **계획과 근거.** 항목마다 질문·데이터·그림·판정 규칙·상태 |
| `paper_experiment/<이름>/` | **run 고정.** 그 결과를 만든 디렉토리 목록과 해시. glob이 아니라 명세다 |
| `paper_figures/` | **논문 그림(PDF)**과 그것을 그리는 스크립트 하나씩 |
| `experiments/EXP-NN_*.md` | **실험 하나의 기록.** 실행 전에 쓴 판정 규칙과 실행 후의 결과 |

**여기에는 수치를 손으로 옮겨 적지 않는다.** 표는 스크립트가 만들고 파일 이름을 적는다.
이 저장소가 가장 자주 겪은 오류가 한 수치를 여러 문서에 복사하고 한 곳만 갱신하는 것이다.

---

## 0. 이 계획을 세우기 전에 한 감사 — 인용하던 것 셋이 틀렸다

2026-08-21에 evaluation 계획을 세우면서 **지금까지 근거로 써 온 수치를 파일을 열어
대조했고, 셋이 낡았거나 조건이 달랐다.** 계획이 이 셋 위에 세워져 있었으므로 계획 자체가
바뀌었다. 전문은 [00_audit.md](00_audit.md).

| 인용해 온 문장 | 실제 |
|---|---|
| "판정 실패 사유의 92~96%가 게이트이고 메모리는 0%다" | **워크로드 수정 이전(EXP-67b) 값.** 수정 후(EXP-73) 45 req/s에서 게이트 71.8% / **메모리 18.8%**이고, 클래스 선호를 끄면 84.9% / 3.4%로 움직인다 |
| "우리 KV 투영이 88.5% 과소예측이고 절대오차가 투영 안 하는 것의 3.6배다" | **2026-07-31 run 하나의 값**이고, 그 문서가 원인으로 지목한 길이 프로파일 오류를 **EXP-48이 바로 고쳤다.** 고친 뒤 다시 잰 적이 없다. 게다가 **EXP-49가 그 항이 거절의 7.2%에만 도달한다**고 쟀다 |
| "iteration time 모델은 맞는다 (관측 50.5 대 예측 50.2 ms)" | **조건 하나, 도착률 하나, 수정 이전 워크로드**의 값이다 |

→ **그래서 "예측 정확도를 그려서 추정기가 좋다는 것을 보인다"는 계획을 버렸다.** 그 축에서
우리가 잘한다는 근거가 약하고, 애초에 **그 예측값이 결정에 거의 도달하지 않는다.** 대신
**B1(무엇이 실제로 결정을 막는가)**과 **B2(우리가 "된다"고 한 것 중 몇 %가 틀렸는가)**로
바꿨다.

---

## 1. 항목 — 각각 질문 한 문장, 그림 한 장

`상태` 열: ✅ 데이터 있음 / ▶ 진행 중 / ○ 계획

### A. 이미 있는 데이터로 그림만 그린다 (클러스터 0시간)

| | 질문 | 데이터 | 상태 |
|---|---|---|---|
| **A1** | 도착률을 올리면 어느 제어평면이 먼저 규칙을 못 지키게 되는가 | `paper_experiment/static_sweep_clean_2026-08/` — 5 arm × 8 도착률 × 2반복 = 80 run | ✅ |
| **A2** | **아무도 거절하지 않을 때, 목적지를 고르는 규칙만으로 얼마나 갈라지는가** | 위 + **EXP-88**(`fsroute` 8 도착률 × 2반복) | ▶ EXP-88 도는 중 |
| **A3** | 우리 요소를 하나씩 끄면 몇 점 잃는가 | EXP-73 / EXP-78 / EXP-69, 3 도착률 × 2반복 | ✅ |
| **A4** | 요청마다 정확한 출력 길이를 알려 주면 얼마나 좋아지는가 | EXP-64, 4 도착률 × 2 arm × 2반복 | ✅ |

### B. 재분석만 (클러스터 0시간)

| | 질문 | 데이터 | 상태 |
|---|---|---|---|
| **B1** | **지금 워크로드에서 무엇이 배치를 막는가** — 게이트 / incumbents / 메모리 / unpredictable | 고정 세트의 FluidServe 16 run의 스케줄러 메트릭 | ✅ |
| **B2** | **"된다"고 판정하고 배치한 것 중 몇 %가 SLO를 깼는가**, 그리고 무엇을 깼는가 | 같은 run의 `metrics.csv` + `scheduler_dispatch.log` | ✅ |
| **B3** | **함대가 낼 수 있는 서로 다른 토큰당 속도가 몇 개인가**, 그리고 그것이 부하로 정해지는가 | 같은 run의 인스턴스별 `gate_allowance_ms` | ✅ *(계획에 없던 것, B1에서 나왔다)* |

### C. 새 실험

| | 질문 | 규모 | 상태 |
|---|---|---|---|
| **C1** | **예산이 클래스마다 다르지 않으면 우리 우위가 사라지는가** | 12~16조건 | ○ 설계 검토 중([03_C1_design.md](03_C1_design.md)) |
| **C2** | **결정 하나에 얼마가 드는가** — 결정 지연과 인스턴스 수 | 계측 추가 + 6조건 | ○ |

### 하지 않기로 한 것과 이유

| | 왜 |
|---|---|
| 예측값 대 실측값 그림 (KV 투영) | **그 항이 거절의 7.2%에만 도달한다**(EXP-49). 재려면 게이트 쪽을 재야 하고 그건 B1 다음에 정한다 |
| completion probability를 요청별로 채점 | 우리 값은 **클래스 하나에 대한 분포**다. 요청별로 채점하면 정의상 틀린 것을 재게 된다 |
| 프로파일에 ±10/20/30% 오차 주입 | **EXP-63에서 시도했고 재려던 것을 못 쟀다** — 한 클래스만 스케일하면 클래스 간 상대 가중치가 같이 바뀌어 정확도가 아니라 분리 세기를 잰 것이 된다. 설계부터 다시 해야 한다 |
| CanWait의 oracle 비교 | 보유의 값은 이미 측정돼 있다(35/45 req/s에서 14.6 / 22.4점, 그리고 **거절 없이는 −9.4로 해롭다**는 것까지) |
| 8/16/32/64 GPU 실측 | 하드웨어가 8 GPU에 Llama-3.1-70B다. C2에서 **결정 함수만 떼어** 벤치마크하고 그렇게 적는다 |

---

## 2. 이 계획이 닫지 못하는 것 — 논문에 한계로 적어야 하는 것 둘

1. **클래스 선호가 45 req/s에서 −7.1점이다**(EXP-73·EXP-78 둘 다). 우리 설계 요소 하나가
   고부하에서 손해다. 원인은 측정돼 있다 — 선호가 클래스를 적은 인스턴스에 모으고 그
   인스턴스가 KV 한계에 먼저 닿는다(메모리 사유 3.4% → 18.8%).
2. **deepresearch가 35 req/s 위에서 llm-d에 진다**(누적 deadline, 45 req/s에서 48.8~50.8 대
   60.5~67.2). 원인이 측정돼 있고(전용 인스턴스 하나에 몰려 그 인스턴스의 prefill 줄이
   76,813~87,845 토큰이 된다) **처방 여섯이 전부 반증됐다.**

## 2.5 밤사이 새로 알게 된 것 — 계획을 다시 쓰게 만들 수 있는 것

[04_pace_diversity.md](04_pace_diversity.md)가 **"부하는 용량의 나쁜 대리 지표다"를 이
데이터로 그대로 쓸 수 없다**는 것을 보였다 — 부하와 허용 속도가 **저부하에서 크게 어긋나고
고부하에서 일치한다.** 일치하는 이유는 **고부하에서 고를 것이 없어지기 때문**이고, 그것이
더 강한 문장을 준다:

> **함대가 제공할 수 있는 서로 다른 토큰당 속도의 개수는 부하가 아니라 클래스가 어디에
> 놓였는지로 정해진다. 이 워크로드에서 그 개수는 세 개(10~20 req/s) → 하나(25 req/s,
> 스크레이프의 72~85%에서 네 인스턴스가 전부 chat의 50 ms) → 두 개(35~70 req/s, 세 대가
> 50 ms이고 한 대가 100 ms)로 움직인다.**

**B1 · B2 · B3가 하나의 사슬이 된다**: 게이트가 탈락 사유의 71~98%다(B1) → 그 게이트가
함대를 세 개에서 두 개의 속도로 접는다(B3) → 느슨한 쪽이 한 대뿐이라 deepresearch가 전부
그리로 가고, 배치된 그 클래스의 22~34%가 첫 토큰 예산을 넘긴다(B2).

## 3. 파일

| 파일 | 무엇 |
|---|---|
| [00_audit.md](00_audit.md) | 계획을 세우기 전에 한 근거 감사 — 확인된 것과 낡은 것 |
| [01_B1_binding_predicate.md](01_B1_binding_predicate.md) | B1의 방법과 결과 |
| [02_B2_feasible_but_missed.md](02_B2_feasible_but_missed.md) | B2의 방법과 결과 |
| [04_pace_diversity.md](04_pace_diversity.md) | **B3 — 계획에 없던 것.** 함대가 낼 수 있는 속도의 개수가 3 → 1 → 2로 움직이고, 2가 됐을 때 느슨한 쪽이 인스턴스 하나뿐이다 |
| [03_C1_design.md](03_C1_design.md) | C1을 confound 없이 만들 수 있는가 |
| [10_figures.md](10_figures.md) | 그림마다 주장 / 스크립트 / 데이터 / **그림이 말하지 않는 것** |
