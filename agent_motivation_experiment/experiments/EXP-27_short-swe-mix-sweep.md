# EXP-27 — swe 입력을 줄이고, 믹스를 입력 토큰 기준으로 정의한다

**상태**: 실행 중 (2026-07-28 04:40 KST 시작, 약 3.9시간). 세션 접두사 `exp27r1`
**스크립트**: `k8s/exp07/run_exp27_mixsweep.sh`
**브랜치**: llumnix `feat/fluidserve` (v19+v20), Agent_applications `feat/fluidserve`

## 1. 왜 이 실험인가 — 지금까지 정적 파티션은 움직일 필요가 없었다

EXP-21~25는 전부 **요청 수 기준 1:1:1** 믹스로 측정했다. 그런데 클래스별 평균 입력이
chat 674 / dr 4,055 / swe 22,474 토큰이므로, 요청 수가 같으면 **입력 토큰의 82.6%가
swe 하나**에 몰린다. PolyServe의 수요 추정은 요청당 server-seconds에 도착률을 곱한
것이고 swe의 요청당 비용이 chat의 약 32배이므로, 어떤 비율을 줘도 수요가 swe에
지배된다.

`allocateServers`를 오프라인으로 재현해 확인했다(프로파일 테이블을 그대로 읽어
`serverSecondsPerRequest`와 largest-remainder 배분을 재구현; 스크립트는
scratchpad에 있고 결과만 옮긴다).

| swe 입력 | 믹스 (요청 수) | 입력 토큰 비율 chat/dr/swe | 배정 (swe/chat/dr) |
|---|---|---|---|
| 22,474 | 1:1:1 | 2 / 15 / 83 | **2 / 1 / 1** |
| 22,474 | 33:6:1 (토큰 1:1:1) | 32 / 35 / 33 | 1 / 2 / 1 |
| 22,474 | 8:1:1 (토큰 1:1:4) | 17 / 13 / 70 | **2 / 1 / 1** |
| **6,812** | 10:2:1 (토큰 1:1:1) | 31 / 37 / 31 | **2 / 1 / 1** |
| **6,812** | 40:2:1 (토큰 4:1:1) | 64 / 19 / 16 | 1 / 2 / 1 |
| **6,812** | 6:1:2 (토큰 1:1:4) | 19 / 19 / 63 | **2 / 1 / 1** |

즉 **믹스를 입력 토큰 비율로 정의하면 배정이 실제로 움직인다.** 요청 수로 정의하는
한 움직이지 않는다. 이것이 EXP-26(엔진을 3대로 줄여 격리를 불가능하게 만든다)을
하지 않고도 같은 질문에 답할 수 있는 방법이다.

## 2. 워크로드 변경 — swe 입력 22,474 → 6,812 토큰

`workloads/codingagent_request_level_poisson/build_short_transcript.py`가 기존
transcript를 변환해 만든다. 재실행 가능하고, 결과 파일은 git 대상이 아니다.

```
records                 1500
system prompt tokens    14,481 -> 4,047
conversation ratio      0.3694
mean input tokens       22,474 -> 6,812   (p10/p50/p90 = 4,227 / 6,569 / 9,578)
structural prefix share 81.5% -> 78.9%
```

**보존한 것과 그 이유**

| 보존 | 이유 |
|---|---|
| prefix 구조 | 모든 변환이 메시지 내용의 순수 함수라, 변환 전에 접두사를 공유하던 두 레코드는 변환 후에도 **동일한 토큰 접두사**를 공유한다. radix cache가 보는 공유 패턴이 규모만 줄어든 채 유지된다. 측정된 구조적 prefix 공유율 81.5% → 78.9% |
| 메시지 수와 역할 | 같은 agent job의 call 1, 2, 3 사이 접두사 관계가 유지된다 |
| 각 메시지의 끝 | 긴 메시지는 **가운데를 잘라낸다**. 모델이 응답할 stage 지시문이 마지막 user 메시지의 끝에 있으므로, 뒤를 자르면 모델이 하는 일 자체가 바뀌고 출력 길이도 바뀐다 |
| 길이 분포의 형태 | system 외 메시지에 단일 비율을 적용 |

**system prompt는 토큰 오프셋으로 자르지 않고 섹션을 골라 남겼다**(Role Definition /
General Principles / Tool Usage Instructions / Output Format Specifications + 마무리
문단). 실제 coding agent의 system prompt가 역할·도구·출력 형식으로 구성된다는 점에서
4,047 토큰은 현실적인 범위 안이고, 문장 중간에서 잘린 프롬프트를 남기지 않는다.

**보존하지 않은 것**: `baseline_ttft_s` / `baseline_tbt_mean_ms` / `baseline_e2e_s`는
원본 긴 프롬프트의 단독 실행 시간이며 짧은 버전에 대해 틀리다. Llumnix 믹스 실행은
`--disable-timeouts`로 `job_timeout_sec`을 0으로 만들고 절대 SLO 임계로 채점하므로
아무도 읽지 않는다. 스키마를 유지하기 위해 필드는 남기고 `baseline_valid: false`를
찍는다.

**KV hit rate가 비현실적으로 높아지지 않는가**: 반대다. 원본이 81.5%로 더 높았고,
그 대부분(14,481/22,474 = 64.4%)이 **모든 요청이 공유하는 하나의 system prompt**에서
왔다. 짧은 버전은 78.9%로 약간 낮다.

## 3. 세 가지 믹스

`workload_configs/mix_short_{m1_balanced,m2_chatheavy,m3_sweheavy}.json`

| 키 | 요청 수 chat:dr:swe | 실현 입력 토큰 비율 | PolyServe 배정 |
|---|---|---|---|
| m1 balanced | 10 : 2 : 1 | 31.1 / 37.4 / 31.4 | 2 swe / 1 chat / 1 dr |
| m2 chat-heavy | 40 : 2 : 1 | 64.4 / 19.4 / 16.3 | **1 swe / 2 chat / 1 dr** |
| m3 swe-heavy | 6 : 1 : 2 | 18.6 / 18.7 / 62.7 | 2 swe / 1 chat / 1 dr |

SLO 블록은 EXP-21/25와 동일하다(chat 5000ms/50ms, dr 10000ms/100ms, swe e2e 30s).
바꾸지 않는 이유는 채점 기준을 이전 실험과 이어 붙이기 위해서다. swe 입력이 3.3배
짧아졌으므로 swe는 **쉬워진다** — 그것이 목적이다. EXP-24에서 swe 달성률이
1800 rpm에 5.6%, 3000 rpm에 1.5%였는데, 그 상태에서는 등가중 지표의 1/3이 상수 0이라
정책 간 차이를 담지 못한다.

## 4. 채점 규약을 바꾼다

지금까지는 **offered 분모**만 봤다(거절·에러·미완을 전부 위반으로). 그 규약에서는
거절하는 정책이 거절하지 않는 정책을 이길 수 있는 구간이 원리적으로 없다.

이제 **두 분모를 나란히 보고한다.**

| | 정의 | 무엇을 왜곡하나 |
|---|---|---|
| **admitted** (주 지표) | 분모 = 시스템이 받아들인 요청 | 단독으로 읽으면 전부 거절하는 정책이 최고점을 받는다 |
| offered | 분모 = 도착한 모든 요청 | 어차피 못 지킬 요청을 거절해도 손실로 계산한다 |

**단독으로 읽지 않기 위한 두 개의 동반 지표**: 거절률(클래스별)과 token goodput.
거절된 요청은 토큰을 만들지 않으므로 과도한 거절은 goodput에서 반드시 드러난다.
목표는 **admitted 기준 모든 rate에서 90% 이상, 동시에 goodput이 PolyServe 이상**이다.

> 구현 결함 하나를 같이 고쳤다. `attain()`이 served 열에서도 거절된 요청을 분모에
> 남겨두고 있었다(거절된 요청은 first token이 없으므로 `miss`가 참). **2026-07-28
> 이전에 기록된 "served" 수치는 전부 사실상 offered 수치다.**

## 5. 설계

```
rep(바깥) x mix(3: m1/m2/m3) x arm(2: polyserve/fluidserve)
rates 1200 / 2400 / 4800 rpm, 조건당 8분, 조건마다 엔진 콜드 재시작
1 rep = 6 셀 x 3 rate x 약 13분 = 약 3.9시간
```

- **반복이 바깥 루프**다. EXP-24에서 세션 내 산포 0.2점, 세션 간 이동 5점이 측정됐고,
  EXP-25에서는 admission을 끈 arm의 반복 간 반폭이 23.6점까지 나왔다.
  arm을 바깥에 두면 그 산포가 arm 효과로 읽힌다.

**rate 1200/2400/4800의 근거 — 별도 calib를 돌리지 않은 이유.** PolyServe의 수요
모델은 chat의 용량을 실측보다 낮게 잡는 것이 이미 알려져 있어(3000 rpm에서 chat
100% 달성) 그 모델로 rate를 정할 수 없다. 대신 **디코드 법칙에서 직접 계산**했다.
짧은 swe에서 m1의 요청당 평균 KV는 약 1,864 토큰이므로

```
step = c0 + c_kv*B*KV + c_n*B = 16.4 + 0.106 B  ms
chat의 50 ms 예산  ->  B <= 317
엔진당 처리량      =  317 / 50 ms = 6,340 출력토큰/s
4대 / 평균 출력 395 토큰  ->  64 req/s = 약 3,850 rpm  (m1의 포화점)
```

같은 계산이 m2에 4,330 rpm, m3에 3,078 rpm을 준다. 세 믹스가 3,000~4,300 사이에
모이므로 **한 rate 리스트(1200 저부하 / 2400 중간 / 4800 과부하)가 셋을 다 커버한다.**
polyserve arm 자체가 calib 역할을 하므로 별도 조건에 시간을 쓰지 않는다.

## 6. 실행 전에 적어두는 판정 규칙

| 관측 | 결론 |
|---|---|
| m2에서 PolyServe의 배정이 실제로 (1,2,1)로 움직임 | 이 워크로드가 원안이 말한 조건을 만든다는 확증. `scheduler_polyserve_tier_servers`로 확인 |
| 배정이 움직이는 동안 PolyServe의 chat/dr이 떨어짐 | 정적 파티션의 전환 비용이 실측됐다는 뜻. 마이그레이션이 꺼져 있으므로 재배정된 서버는 in-flight 요청이 끝날 때까지 두 클래스를 동시에 들고 있다 |
| FluidServe가 admitted ≥ 90%이면서 goodput ≥ PolyServe | 목표 달성 |
| admitted ≥ 90%인데 goodput이 낮음 | 여전히 과도하게 거절하는 것이다. 거절률과 클래스별 거절률을 본다 |
| 세 믹스 전부에서 배정이 안 움직임 | 오프라인 재현이 틀렸다는 뜻. `serverSecondsPerRequest`의 실측 입력(클래스별 평균 입력 토큰)이 내 가정과 다른지부터 확인 |

## 7. 함께 확인할 것

- `scheduler_polyserve_tier_servers` / `scheduler_polyserve_tier_demand` — 배정이
  움직이는 시점과 횟수
- `scheduler_fluidserve_prefill_duty{instance}` — v19가 넣은 계열. 인스턴스마다
  갈라져야 한다. 네 인스턴스가 같은 값이면 라우팅이 클래스를 섞고 있다는 뜻이다
- `engine_occupancy.py` — 엔진이 놀고 있는가
- `slo_rule_breakdown.py` — TTFT 실패인가 TBT 실패인가

## 8. 관련

- 선행: [EXP-25](EXP-25_routing-vs-admission.md) — 라우팅과 admission 분리
- 대안이었던 것: [EXP-26](EXP-26_partition-must-move.md) — 엔진을 3대로 줄이는 안.
  사용자 지시로 보류했고, 이 실험이 같은 질문에 워크로드 쪽에서 답한다
- 정책 변경 근거: `ms_dev/notes/fluidserve-implementation.md` §15 (v19), §16 (v20)

---

## 중간 관측 1 (첫 조건, polyserve / m1 / 1200 rpm)

### 워크로드가 설계대로 나왔다

| 클래스 | 요청 수 | 실측 입력 평균 | 실측 출력 평균 | 설계값 |
|---|---|---|---|---|
| chat | 14,880 | 666 | 439 | 674 |
| deepresearch | 2,952 | 4,074 | 278 | 4,055 |
| swe | 1,488 | **5,930** | **523** | 6,812 (tiktoken 기준) |

- 요청 비율 10 : 1.98 : 1 — m1 목표(10:2:1)와 일치.
- 실현 입력 토큰 비율 32.2 / 39.1 / 28.7% — 목표 31/37/31%와 일치.
- swe 입력이 5,930으로 설계값 6,812보다 13% 낮은 것은 **토크나이저 차이**다.
  생성 스크립트는 cl100k_base로 세고 실측은 서버 쪽 계정이다. 5,930은 사용자가
  제시한 5~7k 범위 안이다.
- **출력 길이가 안 변했다**: swe 523, 원본 transcript의 기록값 평균 560. 메시지를
  가운데에서 자르고 끝의 지시문을 보존한 설계가 의도대로 동작했다는 확인이다.
  뒤를 잘랐다면 모델이 하는 일이 바뀌어 출력 길이가 달라졌을 것이다.

### ⭐ PolyServe의 파티션이 움직인다 — 그것도 예상보다 강하게

옛 워크로드에서 재분할은 **459 샘플 동안 변화 0회**였다. 같은 코드가 이 워크로드에서는
**한 조건 안에서 13회** 바꾼다. 기동 과도구간을 뺀 정상 상태만 보면:

```
t(s)   swe(25) chat(50) dr(100)
  79      2       1       1
 140      1       2       1     <- 이동
 170      2       1       1     <- 30초 만에 복귀
 322      1       2       1
 342      2       1       1     <- 20초 만에 복귀
 433      1       1       2     <- 세 번째 배정
 453      2       1       1     <- 20초 만에 복귀
```

**믹스가 고정인데도 배정이 세 가지 구성 사이를 오간다.** 원인은 수요 추정의 잡음이다.
같은 조건에서 관측된 수요 진폭:

```
 25ms(swe)  min 0.035  median 1.458  max 2.380   (68.7배)
 50ms(chat) min 0.064  median 1.285  max 1.866   (29.3배)
100ms(dr)   min 0.663  median 0.987  max 1.434   ( 2.2배)
```

세 tier의 median 수요가 1.458 / 1.285 / 0.987로 **거의 같다**. 이것이 토큰 비율로
믹스를 정의한 직접적 결과다 — 그리고 largest-remainder 배분은 수요가 비슷할 때
경계에 놓이므로, EWMA(α=0.5)가 도착 잡음을 따라가는 것만으로 정수 배정이 뒤집힌다.

**즉 정적 파티션의 문제는 "느리게 따라간다"가 아니라 "균형점에서 진동한다"이다.**
migration이 꺼져 있으므로 tier가 바뀐 서버는 옛 tier의 in-flight 요청을 끝까지 들고
있고, 20~30초마다 그 상태가 반복된다. EXP-28이 인위적으로 만들려던 조건이
**고정 믹스에서 저절로 발생**하고 있다.

단, 이 조건은 부하가 낮다(KV 1.5~9%, 최대 대기열 8). **진동이 비용을 발생시키는지는
2400/4800 rpm 조건을 봐야 판정된다.** 낮은 부하에서는 두 클래스를 동시에 들어도
예산이 남기 때문이다.

### 확인 필요 — prefix hit rate가 93.8~96.4%로 높다

네 엔진 모두 94% 이상이다. 이것은 워크로드 단축 때문이 아니라 **literal replay와 낮은
부하의 조합**이다: 1,500개 transcript와 1,000개 대화를 순환 재생하는데 KV가 1.5~9%밖에
안 차서 캐시가 아무것도 축출하지 않는다. 옛 워크로드에서도 KV 압력이 없던 엔진은
94.5%였고, 압력이 있던 엔진만 66%로 떨어졌다.

**높은 rate 조건에서 이 값이 떨어지는지 확인해야 한다.** 전 rate에서 95%면 이 실험의
prefill 비용이 비현실적으로 작다는 뜻이고, 그때는 풀 크기를 키우는 것(transcript
레코드 수, 대화 수)을 검토해야 한다.

---

## 9. pass 2 — 반복과 v20 귀속 (pass 1 종료 후 실행)

**스크립트**: `/home/nxclab/tools/exp27_pass2.sh <mix> [rates] [min] [reps]`

pass 1은 세 믹스를 1회씩 훑어 **어디서 두 정책이 갈리는지**를 찾는다. 그것으로는
정작 중요한 두 가지가 판정되지 않는다.

| 질문 | 왜 pass 1로는 안 되나 |
|---|---|
| 산포가 얼마인가 | EXP-25에서 FluidServe 라우팅의 반복 간 반폭이 1800 rpm에서 **23.6점**이었다(PolyServe 0.1). 그보다 작은 1회 차이는 아무 말도 하지 않는다 |
| v20이 무엇을 했나 | v20의 주장은 라우팅 래칫을 멈춘다는 것이고, **1차 판정 기준은 평균이 아니라 산포**다. 같은 조건을 항 on/off로 측정해야 한다 |

**arm 3개, 반복이 바깥 루프, 믹스는 하나**(pass 1이 지목한 것):

| arm | 설정 | 답하는 것 |
|---|---|---|
| polyserve | — | 기준선 |
| fluidserve | v19 + v20 | 현재 상태 |
| fluidserveflat | `FS_CLASS_HARM=false` (v19만) | v20의 기여. damage 추정이 "아직 예산을 지키는 incumbent의 합"으로 되돌아가, 자기 클래스가 이미 늦기 시작한 인스턴스가 0으로 읽힌다 |

`FS_AFFINITY=false`와 별개의 스위치인 이유: affinity는 feasible 집합의 정렬을
끄는데, **v20이 작동하는 영역은 feasible이 하나도 없는 영역**이라 그 정렬은 애초에
실행되지 않는다. 한 스위치로 재면 둘의 합을 보고하게 된다.

바이너리는 `scheduler-exp07-v20a`(스위치 포함)이고, 스크립트가 설치 후
**파드 안에서 `md5sum /proc/1/exe`를 읽어 실행 중인 바이너리를 대조**한다.
파일 시각이나 spec으로는 잡히지 않는 실패를 잡는 유일한 방법이다.

---

## 중간 관측 2 (polyserve / m1 전 rate 완료) — 실패 클래스가 뒤집혔다

| rpm | admitted | offered | goodput | chat | dr | swe |
|---|---|---|---|---|---|---|
| 1200 | 99.7 | 99.7 | 8,504 | 100.0 | 100.0 | 99.2 |
| 2400 | 79.0 | 79.0 | 7,181 | **37.1** | 100.0 | 100.0 |
| 4800 | 70.8 | 70.8 | 9,733 | **13.9** | 100.0 | 98.5 |

**옛 워크로드에서는 chat이 항상 100이고 swe가 0.5~5였다. 여기서는 정확히 반대다.**
swe가 98~100이고 **chat이 무너진다.**

### 원인 — 정적 배정이 chat을 극단적으로 과소 배정한다

m1은 요청 수 기준 chat 10 : dr 2 : swe 1이므로 **chat이 전체 요청의 76.9%**다.
그런데 PolyServe의 수요 추정은 요청당 server-seconds에 도착률을 곱하므로

```
chat  0.769 x 0.080 = 0.062
swe   0.077 x 1.570 = 0.121   <- chat의 2배
dr    0.154 x 0.311 = 0.048
```

배정은 (swe 2 / chat 1 / dr 1)이 되고, **요청의 77%가 서버 1대에 들어간다.**

### 실패 형태는 큐잉이다 — 지연이 아니라

`slo_rule_breakdown.py`, 2400 rpm:

```
chat  n=14,134  완료 80.7%
  TTFT <= 5.0s :  32.3%   p50 13.22s  p90 56.12s
  TBT  <=  50ms:  55.4%   p50  48.3ms p90  52.8ms
dr    TTFT 100% (p50 0.37s),  swe E2E 100% (p50 10.0s)

chat TTFT 중앙값의 시간 추이: 0.28 -> 0.50 -> 5.33 -> 25.15 -> 48.60 -> 61.97 s
```

**TBT는 48.3ms로 50ms 예산 안에 있다. 디코드는 멀쩡하고 요청이 그냥 기다린다.**
그리고 대기 시간이 단조 증가한다 — 서버 1대의 처리량을 도착률이 넘어섰다는 뜻이다.

엔진 점유가 이를 확인한다(2400 rpm):

```
engine  preemptions  prefix hit   KV mean  KV max
8000              0        2.3%    48.4%    81.1%   <- 여기만 일한다
8001              0       91.7%     4.0%    13.1%
8002              0       94.1%     2.7%    12.2%
8003              0       93.3%     2.8%    14.4%
```

**한 엔진이 KV 48%로 일하는 동안 나머지 셋은 2.7~4.0%다.** 지연 예산이 남아도는
엔진 세 대를 두고 chat이 한 대 뒤에서 62초를 기다린다.

### 이것이 FluidServe가 이겨야 하는 조건이다

FluidServe는 서버를 클래스에 배정하지 않고 **지연 예산으로만 게이트**한다.
chat의 TBT가 48ms로 예산 안이라는 것은, 남는 엔진들이 chat을 더 받아도 예산을
지킬 수 있다는 뜻이다. 배정이 없으면 chat은 그 엔진들로 흘러간다.

**만약 여기서도 FluidServe가 지면, 문제는 워크로드나 채점이 아니라 라우팅 규칙
자체다.** 지금까지 중 가장 명확한 시험대다.

### 부수 관측 — PolyServe의 knee가 1200과 2400 사이다

goodput이 1200에서 8,504, 2400에서 7,181로 **떨어진다**(4800에서 9,733으로 회복).
2400에서 chat 토큰의 63%가 SLO를 못 맞춰 버려지기 때문이다. rate 리스트가 knee를
사이에 두고 있어 목적에 맞는다.

---

## ⭐ 중간 관측 3 (m1, 두 arm 완료) — 2400 rpm에서 두 지표 모두 이긴다

| arm | rpm | admitted | offered | 거절% | goodput | chat / dr / swe |
|---|---|---|---|---|---|---|
| polyserve | 1200 | 99.7 | 99.7 | 0.0 | 8,504 | 100.0 / 100.0 / 99.2 |
| **fluidserve** | 1200 | **100.0** | 100.0 | 0.0 | 8,564 | 100.0 / 100.0 / 100.0 |
| polyserve | 2400 | 79.0 | 79.0 | 0.0 | 7,181 | 37.1 / 100.0 / 100.0 |
| **fluidserve** | 2400 | **100.0** | **100.0** | **0.0** | **16,533** | 100.0 / 100.0 / 100.0 |
| polyserve | 4800 | **70.8** | 70.8 | 0.0 | 9,733 | 13.9 / 100.0 / 98.5 |
| fluidserve | 4800 | 60.5 | 32.4 | 38.0 | **10,793** | 51.9 / 76.8 / 52.9 |

```
polyserve 대비 fluidserve, rate별
  rpm   admitted   offered   goodput
 1200       +0.3      +0.3       +60
 2400      +21.0     +20.9    +9,352      <- 두 지표 모두, 거절 0으로
 4800      -10.3     -38.4    +1,060
```

### 2400 rpm — 거절 없이 완승이다

**admitted 100.0 대 79.0, goodput 16,533 대 7,181(2.3배), 거절률 0.0%.**
offered 분모로 봐도 같다(거절이 없으므로 두 분모가 일치한다). 즉 **채점 규약을
바꿔서 얻은 결과가 아니다.**

내부 정합성 확인: 40 req/s × 평균 출력 395 토큰 = 15,800 tok/s가 이론 상한이고
16,533은 사실상 **전 요청이 SLO를 만족하고 토큰을 생산했다**는 뜻이다.
PolyServe의 7,181은 상한의 45%로, 요청의 77%인 chat이 37.1%인 것과 맞는다.

메커니즘도 확인된다:

```
                    engine  prefix hit  KV mean  최대 대기열  idle-while-queued
polyserve 2400        8000        2.3%    48.4%       (한 대에 집중)
                      8001       91.7%     4.0%
                      8002       94.1%     2.7%
                      8003       93.3%     2.8%
fluidserve 2400       8000       84.9%    12.3%           22           0.7%
                      8001       87.1%    13.9%
                      8002       91.1%     4.6%
                      8003       91.7%     5.4%
```

**PolyServe는 한 엔진을 KV 48%로 몰고 세 대를 2.7~4.0%로 놀린다. FluidServe는 네 대를
4.6~13.9%로 고르게 쓴다.** chat의 TBT가 48ms로 예산 안이었다는 것이 곧 "남는 엔진이
chat을 받아도 된다"는 뜻이었고, 서버 배정이 없는 정책은 그것을 이용한다.

### v19가 의도대로 작동한다 — `prefill_duty`가 인스턴스마다 갈린다

```
2400 rpm  인스턴스별 prefill duty 중앙값:  0.010 / 0.028 / 0.063 / 0.120  (12배 차이)
4800 rpm                                   0.168 / 0.171 / 0.169 / 0.242
```

**옛 fleet 평균 항이었다면 네 값이 정의상 같다.** 2400에서 12배로 갈린다는 것은
투영이 실제로 인스턴스를 구분하는 신호가 됐다는 직접 증거다. 4800에서 수렴하는 것은
fleet이 균일하게 포화됐기 때문이고, 그 조건에서 지는 것과 일치한다.

### 4800 rpm에서 지는 이유 — 게이트가 아직 너무 조인다

```
chat  n=28,305  완료 63.3%   TTFT<=5s: 33.8% (p50 4.81s)   TBT<=50ms: 60.3% (p50 28.7ms)
dr    n= 5,669  완료 63.0%   TTFT<=10s: 48.4% (p50 7.36s)  TBT<=100ms: 63.0% (p50 28.1ms)
swe   n= 2,832  완료 28.2%   E2E<=30s: 14.9% (p50 29.4s)
엔진 KV 평균 20.9~21.6%, 최대 대기열 56, idle-while-queued 0.0%
```

**TBT는 28.7ms로 예산의 절반이고 엔진 KV는 21%다. 엔진은 여유가 있는데 요청이
게이트웨이에서 기다린다.** 38%를 거절하고도 받아들인 것의 37%가 완료되지 못한다.

즉 4800에서는 **보유(PEND)가 너무 길고 게이트가 너무 조여** 요청이 TTFT를 대기로
소진한다. v19가 2400을 고쳤지만 이 rate에서는 여전히 남아 있다. 다음 수정 대상이다.

### ⚠ 이 결과는 n=1이다

2400 rpm의 +21.0점 차이는 EXP-25에서 관측된 최대 반폭(23.6점)보다 작다. 다만
(a) FluidServe가 100.0/100.0으로 **상한에 붙어 있어** 위쪽 산포가 구조적으로 불가능하고,
(b) goodput 2.3배는 지금까지 측정된 어떤 산포보다 크며,
(c) 거절이 0이라 admission 관련 산포 원인이 없다.
그래도 **pass 2의 반복으로 확인하기 전까지는 확정이 아니다.**

---

## 중간 관측 4 — prefix hit rate 우려는 해소됐다

중간 관측 1에서 "1200 rpm에서 네 엔진 전부 94~96%면 비현실적으로 높다"고 적었다.
부하가 올라가면 떨어진다.

| 조건 | 엔진별 prefix hit |
|---|---|
| 1200 rpm | 93.8 / 95.4 / 96.4 / 96.4 % |
| 2400 rpm (fluidserve) | 84.9 / 87.1 / 91.1 / 91.7 % |
| 4800 rpm (fluidserve) | 71.8 / 72.3 / 72.7 / 73.6 % |

KV 압력이 생기면 radix cache가 축출을 시작하고 재사용률이 떨어진다. **높은 값은
저부하 구간에만 나타나고, 그 구간은 두 정책이 모두 100%를 내는 구간이라 결론에
영향을 주지 않는다.** transcript 풀을 키울 필요는 없다.

## 중간 관측 5 (polyserve / m2 chat-heavy) — 오프라인 재현이 맞았고, 배정이 안정적이다

| rpm | admitted | goodput | chat / dr / swe |
|---|---|---|---|
| 1200 | 100.0 | 9,035 | 100.0 / 100.0 / 100.0 |
| 2400 | 100.0 | 16,715 | 100.0 / 100.0 / 100.0 |
| 4800 | 71.7 | 5,709 | **15.1** / 100.0 / 100.0 |

**배정이 (swe 1 / chat 2 / dr 1)로 나온다** — §1의 오프라인 `allocateServers` 재현이
예측한 그대로다. 그리고 **m1과 달리 진동하지 않는다**: t=56~66초에 자리를 잡은 뒤
조건이 끝날 때까지 한 번도 바뀌지 않는다(세 rate 모두).

두 믹스의 대비가 이 실험의 핵심이다.

| | 세 tier의 수요 | 배정 | 거동 |
|---|---|---|---|
| m1 balanced | 1.458 / 1.285 / 0.987 (거의 동일) | (2,1,1) | **20~30초마다 진동**, chat이 1대에 갇혀 2400에서 37.1%로 붕괴 |
| m2 chat-heavy | chat이 지배 | (1,2,1) | **안정**, chat이 2대를 받아 2400까지 100% |

**즉 정적 파티션은 한 클래스가 명확히 지배할 때는 잘 동작하고, 클래스들이 균형을
이룰 때 무너진다.** 균형이 정확히 "어느 클래스에도 전용 서버를 줄 이유가 없는" 상황,
즉 소프트 비닝이 필요한 상황이다.

4800 rpm에서는 m2도 chat이 15.1%로 무너지고 goodput이 5,709로 떨어진다(m1의 9,733보다
낮다 — m2는 요청의 93%가 chat이라 chat 붕괴가 곧 전체 붕괴다).

---

## ⭐ 중간 관측 6 — 등가중 집계가 이 믹스들에서는 오도한다. 채점을 하나 더 붙인다

m2의 4800 rpm에서 **두 집계 방식이 정반대를 가리킨다.**

| m2 4800 rpm | 등가중(클래스) | 요청당 | goodput | chat / dr / swe |
|---|---|---|---|---|
| polyserve | **71.7** | **22.3** | 5,709 | 15.1 / 100.0 / 100.0 |
| fluidserve | 57.5 | **53.9** | **10,902** | 52.7 / 79.9 / 40.1 |
| **차이** | **PolyServe +14.2** | **FluidServe +31.6** | **FluidServe 1.9배** | |

원인은 산술이다. **m2는 요청의 93%가 chat**이고 dr 4.7%, swe 2.3%다.
PolyServe는 chat을 15.1%로 떨어뜨리면서 dr/swe를 100%로 지킨다. 등가중은
`(15.1+100+100)/3 = 71.7`로, **트래픽의 4.7%와 2.3%를 지닌 두 클래스에게 93%를 지닌
클래스와 같은 발언권**을 준다. 요청당으로 세면 `0.930×15.1 + 0.047×100 + 0.023×100
= 22.3`이다.

**등가중은 믹스가 요청 수 기준 1:1:1이던 동안에는 요청당 집계와 같은 값이었다.**
EXP-21~25가 그 조건이었기 때문에 하나만 보고해도 문제가 없었다. 믹스를 토큰 비율로
정의하면서 클래스별 요청 수가 40배까지 벌어졌고, 그 순간부터 둘은 다른 값이다.

**둘 다 보고한다. 어느 쪽도 단독으로는 옳지 않다.**

| | 무엇을 답하나 | 무엇을 가리나 |
|---|---|---|
| 등가중 (클래스) | 굶는 서비스 tier가 있는가 | 지배적 클래스가 굶어도 건강한 소수 클래스 둘 뒤에 숨는다 |
| 요청당 | 트래픽의 몇 %가 약속받은 것을 받았나 | 지배적 클래스가 건강하면 굶는 소수 클래스가 묻힌다 |

**token goodput은 이 선택에 의존하지 않는다**(정의상 요청 수로 가중된다). 그리고
m2 4800에서 goodput은 FluidServe 1.9배로 요청당 집계와 같은 방향을 가리킨다.

### 두 믹스 전체 (pass 1, n=1)

```
m1 balanced        polyserve - fluidserve
   rpm   eq-adm  eq-off  req-adm  req-off   goodput
  1200     -0.3    -0.3     -0.1     -0.1       -60
  2400    -21.0   -20.9    -45.7    -45.7    -9,352     <- 모든 지표 FluidServe
  4800    +10.3   +38.4    -19.2     +2.4    -1,060

m2 chat-heavy      polyserve - fluidserve
   rpm   eq-adm  eq-off  req-adm  req-off   goodput
  1200     +0.3    +0.3     +0.0     +0.0       -28
  2400     -0.0    -0.0     -0.0     -0.0       -45     <- 둘 다 천장
  4800    +14.2   +39.4    -31.6    -10.8    -5,194
```

**정리하면:**

- **1200 rpm**: 둘 다 100. 차이 없음.
- **2400 rpm**: m1에서 FluidServe가 **거절 0으로** 모든 지표를 이긴다
  (요청당 −45.7점, goodput 2.3배). m2에서는 둘 다 천장이라 무승부.
- **4800 rpm**: **등가중으로는 PolyServe, 요청당과 goodput으로는 FluidServe.**
  PolyServe는 소수 클래스 둘을 완벽히 지키면서 실제 트래픽의 85%를 실패시키고,
  FluidServe는 손해를 고르게 펴면서 토큰을 1.9배 만든다.

**따라서 "모든 rate에서 90% 이상 + goodput ≥ PolyServe"는 아직 달성되지 않았다.**
4800 rpm에서 FluidServe의 요청당 admitted가 53.9(m2)/55.9(m1)로 90%에 못 미친다.
원인은 이미 특정돼 있다(중간 관측 3): 엔진 KV가 21%이고 TBT가 예산의 절반인데
게이트가 조여 요청이 게이트웨이에서 TTFT를 소진한다.

---

## pass 1 결과 전체 (18/18 완료, n=1, 2026-07-28)

바이너리는 v19+v20(v21 이전). 표기: `eq` 등가중(클래스), `req` 요청당, `adm` admitted
분모, `off` offered 분모.

| mix | rpm | arm | eq-adm | eq-off | req-adm | req-off | 거절% | goodput | chat/dr/swe |
|---|---|---|---|---|---|---|---|---|---|
| m1 | 1200 | polyserve | 99.7 | 99.7 | 99.9 | 99.9 | 0.0 | 8,504 | 100/100/99.2 |
| m1 | 1200 | fluidserve | 100.0 | 100.0 | 100.0 | 100.0 | 0.0 | 8,564 | 100/100/100 |
| m1 | 2400 | polyserve | 79.0 | 79.0 | 54.3 | 54.3 | 0.0 | 7,181 | 37.1/100/100 |
| m1 | 2400 | **fluidserve** | **100.0** | **100.0** | **100.0** | **100.0** | **0.0** | **16,533** | 100/100/100 |
| m1 | 4800 | polyserve | **70.8** | 70.8 | 36.7 | **36.7** | 0.0 | 9,733 | 13.9/100/98.5 |
| m1 | 4800 | fluidserve | 60.5 | 32.4 | **55.9** | 34.4 | 38.0 | **10,793** | 51.9/76.8/52.9 |
| m2 | 1200 | polyserve | 100.0 | 100.0 | 100.0 | 100.0 | 0.0 | 9,035 | 100/100/100 |
| m2 | 1200 | fluidserve | 99.7 | 99.7 | 100.0 | 100.0 | 0.0 | 9,064 | 100/100/99.1 |
| m2 | 2400 | polyserve | 100.0 | 100.0 | 100.0 | 100.0 | 0.0 | 16,715 | 100/100/100 |
| m2 | 2400 | fluidserve | 100.0 | 100.0 | 100.0 | 100.0 | 0.0 | 16,759 | 100/100/100 |
| m2 | 4800 | polyserve | **71.7** | **71.7** | 22.3 | 22.3 | 0.0 | 5,709 | 15.1/100/100 |
| m2 | 4800 | fluidserve | 57.5 | 32.3 | **53.9** | **33.1** | 37.8 | **10,902** | 52.7/79.9/40.1 |
| m3 | 1200 | polyserve | 100.0 | 100.0 | 100.0 | 100.0 | 0.0 | 8,734 | 100/100/100 |
| m3 | 1200 | fluidserve | 100.0 | 100.0 | 100.0 | 100.0 | 0.0 | 8,773 | 100/100/100 |
| m3 | 2400 | polyserve | 84.7 | 84.7 | 70.6 | 70.6 | 0.0 | 11,409 | 54.7/100/99.5 |
| m3 | 2400 | **fluidserve** | **99.6** | **99.0** | **99.7** | **99.4** | 0.4 | **16,964** | 100/100/98.8 |
| m3 | 4800 | polyserve | 55.5 | 55.5 | 32.1 | 32.1 | 0.0 | 8,218 | 9.6/100/56.9 |
| m3 | 4800 | **fluidserve** | **67.4** | 35.1 | **61.0** | **34.5** | 43.3 | **10,949** | 57.5/80.3/64.4 |

### 정리

| rate | 결과 |
|---|---|
| **1200** | 셋 다 무승부(양쪽 100). 저부하에서는 라우팅이 문제되지 않는다 |
| **2400** | **m1과 m3에서 FluidServe가 모든 지표로 이긴다.** m1 요청당 +45.7점·goodput 2.3배(거절 0.0%), m3 +29.1점·goodput 1.49배(거절 0.4%). m2는 둘 다 천장이라 무승부 |
| **4800** | **요청당과 goodput은 세 믹스 모두 FluidServe**(goodput +1,060 / +5,194 / +2,731). 등가중은 m1·m2에서 PolyServe, m3에서 FluidServe |

**2400 rpm 결과가 이 실험의 핵심이다.** 거절이 0인 상태에서 두 지표 모두, 두 분모
모두, 두 집계 방식 모두 이긴다. **채점 규약 변경에 기대지 않는 결과**다.

**4800 rpm에서 등가중이 갈리는 이유**는 중간 관측 6에 적었다: PolyServe가 트래픽의
4.7%와 2.3%를 지닌 두 클래스를 100%로 지키면서 93%를 지닌 클래스를 15%로 떨어뜨리고,
등가중이 그것을 71.7로 읽는다. goodput은 이 선택에 의존하지 않고 FluidServe가 1.9배다.

### 목표 대비

사용자 목표는 **모든 rate에서 attainment ≥ 90% + goodput ≥ PolyServe**다.

- 1200, 2400: **달성**(100.0 / 100.0, goodput 우세).
- 4800: **미달**. 요청당 admitted가 53.9~61.0으로 90%에 못 미친다.

4800의 원인은 특정됐고(§중간 관측 7) v21로 고쳤다. pass 2가 그 효과를 잰다.

---

## pass 2 결과 (18/18 완료, 반복 2회, 바이너리 v21)

m1 balanced. arm 3개: polyserve / fluidserve(v21) / fluidserveflat(`FS_CLASS_HARM=false`).

| arm | rpm | req-adm | req-off | eq-adm | goodput | 거절% |
|---|---|---|---|---|---|---|
| A polyserve | 1200 | 100.0 ±0.0 | 100.0 | 100.0 | 8,563 | 0.0 |
| | 2400 | 55.3 ±0.3 | 55.3 | 79.6 | 7,490 | 0.0 |
| | 4800 | 32.4 ±0.2 | 32.4 | **69.0** | 8,434 | 0.0 |
| B fluidserve | 1200 | 100.0 ±0.0 | 100.0 | 100.0 | 8,526 | 0.0 |
| | **2400** | **100.0 ±0.0** | **100.0** | **100.0** | **16,540** | **0.0** |
| | 4800 | **62.2 ±0.2** | 41.2 | 68.3 | **12,590** | 33.1 |
| C flat | 4800 | 62.7 ±0.9 | 41.7 | 68.2 | 12,697 | 32.8 |

### 판정 (사전 등록 규칙 3개에 대해)

**① 2400의 압승이 재현되는가 → 예, 확정.** 100.0 ±0.0 대 55.3 ±0.3, goodput 2.21배,
거절 0.0%. 거절이 없으므로 두 분모·두 집계가 모두 같은 값이다.

**② 4800의 요청당 admitted가 90%를 넘는가 → 아니오(62.2).** v21이 개선은 했다
(55.9 → 62.2, goodput 10,793 → 12,590, 거절 38.0% → 33.1%, 대조군은 반대 방향으로
1.8 이동). 등가중으로는 PolyServe와 동률이 됐다. **그러나 pend가 86.8% → 85.5%로
거의 그대로**여서, "3.0 ms 반올림이 4800 실패의 전부"라던 중간 진단은 철회한다.

**③ flat과의 산포 차이 → 없음.** 62.7 ±0.9 대 62.2 ±0.2. v20은 이 워크로드에서
측정 가능한 효과가 없다. 근거였던 ±23.6 산포는 옛 워크로드의 것이고 여기서는
그 불안정성 자체가 없다.

### 4800의 실제 원인 — 보유가 TTFT 예산을 먹는다

받아들인 요청의 TTFT:

| rate | 클래스 | TTFT p50 | 예산 | 실패율 |
|---|---|---|---|---|
| 2400 | chat | 0.28 s | 5 s | 0.0% |
| 4800 | chat | **4.08 s** | 5 s | **42.2%** |
| 4800 | dr | 5.51 s | 10 s | 15.9% |

**4800에서 chat이 예산의 82%를 대기로 쓴 뒤 도착한다.** 같은 조건에서 엔진 KV 21%,
ITL은 예산의 절반이다. TTFT로 채점되는 클래스에 대해 **보유는 자기 목적을 배반한다.**

용량 대비: m1의 디코드 한계가 약 3,850 rpm이므로 4800은 1.25배다. 25%를 거절하면
남은 것을 100% 지킬 수 있어야 하는데 33%를 거절하고도 38%가 실패한다 —
**구조적 한계가 아니라 아직 낭비다.**

## 그림

`results/aggregate_analysis/exp27/` (생성: `analysis_scripts/request_level/exp27_figures.py`)

| 파일 | 내용 |
|---|---|
| `exp27_sweep_pass1.png` | 믹스 3패널, 좌축 attainment / 우축 goodput |
| `exp27_sweep_pass2_m1.png` | v21 + 반복 2회, 오차막대 = min..max |
| `exp27_engines_m1_2400.png` | 엔진 4대의 디코드 배치 / 대기열 / KV 시간축 |
| `exp27_latency_m1.png` | 클래스별 TTFT·ITL CDF와 예산선 |
