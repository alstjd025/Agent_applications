# EXP-107 — class-instance cap과 force 분기 제거, mix-shift 한 시간 trace

2026-08-27 작성 (실행 전). 바이너리 `092edd73` (EXP-107 판, v0.3 `d867351a`에
두 메커니즘을 플래그 뒤로 더한 것 — 기본값은 cap 꺼짐 / force 켜짐이라 플래그를
안 만지는 arm의 동작은 v0.3과 코드 경로가 같다). 백업
`bin-backup/scheduler-exp07.pre-exp107` = `d867351a`.

## 0. 무엇을 묻나

**질문 1 (cap)**: 클래스가 gate를 정하는 인스턴스 수를 수요에서 유도한 상한으로
묶고, 상한 초과 상태에서는 그 클래스를 "가장 많이 든 상위 K대"로만 보내 잉여
인스턴스가 한 체류 시간 안에 배수되게 하면 — footprint가 과거 수요의 최댓값이
아니라 현재 수요를 따라가게 되는가, 그리고 그것이 chat-heavy 구간에서
deepresearch·swe의 offered 달성률을 올리는가.

**질문 2 (force)**: "요청 자신은 지킬 수 있지만 모든 배치가 돌고 있는 요청들의
보호 조건을 위반"할 때 최소 피해 인스턴스에 강제 배치하던 분기를 명시적 조기
거절(shed reason `no_feasible`)로 바꾸면 무엇이 달라지는가. 사전 측정(EXP-105
run들의 재분석, `force_fate.py`): 강제 배치의 자기-예산 달성 56~82%, 같은 순간
다른 인스턴스 대비 동거 요청 위반율 수 배; force는 요청의 2.0~3.3%.

## 1. arm — 3개 × 2반복, shift trace, 대조군은 EXP-104

| arm | 정의 |
|---|---|
| (대조) `fsv3` | **재실행 안 함** — EXP-104의 두 run (`260826_0705_exp104r1_fsv3_shift`, `260826_0931_exp104r2_fsv3_shift`) |
| `fsv3cap` | fsv3 + `FS_INSTANCE_CAP=true FS_CAP_WINDOW_MULT=3.0 FS_FORCE=true` |
| `fsv3nofrc` | fsv3 + `FS_INSTANCE_CAP=false FS_FORCE=false` |
| `fsv3capnofrc` | 둘 다 |

swe 예산은 30초 유지(`FS_SWE_E2E_MS`·`FS_SWE_E2E_S` 둘 다 미설정 — EXP-105의
결론대로 40초는 채택하지 않음). trace는
`dyn60_shift_m2Am1B_b1045.csv`(EXP-104와 동일 파일, 드라이버에서 확인).
드라이버는 `run_exp107_capforce.sh`(run_exp105 스냅샷 + 새 env 3개를 unset
목록에 추가 + 새 arm 셋).

## 2. 대조군 비교의 유효성 단서 둘 (실행 전에 적는다)

- **세션 교차**: EXP-104는 다른 세션이다. 반복 규칙대로, EXP-104 fsv3의 반복
  폭보다 큰 차이만 읽는다 — admitted 0.1 / 전체 offered 1.8 / chat 2.9 /
  dr 3.7 / swe 5.4 (EXP-104 §2). **goodput은 EXP-104 문서에서 철회됐으므로
  복사 금지, 정의를 명시하고 재계산한다.**
- **바이너리 교차**: 대조군은 `d867351a`, 처리군은 `092edd73`. 플래그 기본값이
  옛 동작을 재현한다는 것은 코드 검토 + 전체 단위 테스트 통과 + smoke A(아래)로
  확인하고, smoke A의 결과가 이상 신호를 주면 fsv3 재실행 1 run을 추가한다.

## 3. smoke (본 실험 전, 9분 ablation trace)

- **smoke A** `fsv3` — 새 바이너리에서 무변경 설정. 기동 줄에
  `instancecap=false, capmult=3.0, enableforce=true` 확인.
- **smoke B** `fsv3capnofrc` — 계측 확인이 목적:
  `grep -c scheduler_fluidserve_instcap_limit <run>/server_metrics/scheduler.jsonl`
  > 0 (카운터는 생성 시 0으로 사전 등록되므로 "발화 안 함"과 "수집 안 됨"이
  구분된다), `shed_reason_total` 존재, `instcap_limit` 값이 1~4 범위(50짜리
  헛값이면 μ 유도 결함).

## 4. 판정 규칙 (사전 등록)

**H1 (기제 — cap이 결정을 바꾼다, run 안에서 닫힘)**:
`instcap_blocked_feasible_total > 0`이고, 함대가 내는 서로 다른 허용 속도의
개수(pace 다양성)가 대조군이 1로 무너지는 구간에서 ≥ 2로 유지된다.
**반증 조건: cap이 결정을 바꾼 5초 창이 전체의 5% 미만이면 죽은 변수** —
논문에서 mechanism이 아니라 Discussion으로 강등하고 c_win sweep도 하지 않는다.

**H2 (결과 — 재분배)**: chat-heavy 구간(s0·s2)에서 dr offered +4점(폭 3.7)
이상 또는 swe offered +5.5점(폭 5.4) 이상. **전체 offered는 ±3점 안이면 성공**
— cap은 용량을 만들지 않고 거절의 배치를 수요에 정렬시키는 재분배 수단이라고
사전에 주장했으므로, 총계 상승을 성공 조건으로 걸지 않는다.

**H3 (전환 비용)**: 구간 경계 ±τ(chat 64초, dr 120초) 창의 offered 달성률이
대조군 대비 −3점 이내. 넘으면 창 유도(c_win) 재설계 전까지 cap을 채택하지
않는다.

**H4 (force)**: `fsv3nofrc`의 거절률이 +2±1점(force가 2.0%였으므로), admitted
달성률이 오르거나 유지, 전체 offered −1.5점 이내. `shed_reason_total`의
no_feasible 몫이 EXP-105의 force 수(1,949/시간)와 같은 자릿수. force 인구의
동거-피해가 실재했다면 admitted 쪽 개선이 손실 일부를 상쇄한다 — 상쇄가 0이어도
정책 일관성(원안 설계 회귀)만으로 채택할 수 있으나, 전체 offered가 −1.5점을
넘게 나빠지면 채택하지 않고 기본값을 유지한다.

**H5 (상호작용)**: `fsv3capnofrc` − fsv3 ≈ (fsv3cap − fsv3) + (fsv3nofrc −
fsv3) ± 3점이면 두 메커니즘은 독립. cap이 force 발화 지점(막힌 클래스)을 늘리는
방향의 상호작용이 예상되므로, 합보다 나쁘면 그 몫을 shed_reason 분해로
확인한다.

## 5. 실행

```bash
cd Agent_applications/agent_motivation_experiment/k8s/exp07
# 체인은 /home/nxclab/tools/exp107_capforce.sh (setsid, watcher는 PID로)
SESSION_PREFIX_BASE=exp107 ARMS="fsv3cap fsv3nofrc fsv3capnofrc" \
  bash run_exp107_capforce.sh shift 2
```

6 run × ~75분 ≈ 8시간. 디스크: run당 5.6 GB × 6 = 34 GB, 여유 1.1 TB (확인
2026-08-27). 결과 glob은 `results/*exp107r[12]_*` — exp107smoke를 제외하려면
`r[12]`가 필수.

## 5.5 반복 1 중간 진단과 guardrail 수정 (2026-08-28 새벽, 체인 진행 중 기록)

**fsv3cap 반복 1** (260827_0703): offered 71.4 (대조군 76.6/78.5, −6.2), dr **+8~12**
(66.3/62.7 → 74.4), chat −9.6(전부 거절 +9.8, 토큰당 위반 불변 0.6→0.4, admitted
99.4), swe admitted 88.8 → **70.9**. 진단(`results/aggregate_analysis/exp107_capforce/
diagnose_cap_r1.py`, `diag_cap_state.png`):

1. **희소 정규화가 상시 체제였다.** dr·swe의 인스턴스당 처리율이 낮아 원시 수요
   합이 함대 4대에 대해 상시 12~14였고, 비례 몫이 chat을 λ 10~30 req/s 내내
   **상한 1**에 고정했다(17분 이후 거의 전부). 설계는 정규화를 예외 상태로
   가정했는데 부하 있는 모든 순간이 그 상태였다 — cap이 guardrail이 아니라 상시
   파티션으로 동작했고, 그것이 chat 거절 +10점의 직접 원인이다.
2. **swe admitted 하락(s1에서 miss 37.5% 대 대조군 10~13%)은 전부 디코드 시간이다**
   (miss의 TTFT 중앙값 0.9초, 디코드 33초, 출력 ~577토큰 → ~57 ms/token).
   swe의 nominal pace 57.7 ms는 평균 출력로 유도된 값이라 중앙값 요청도 그
   pace로는 간신히다. 대조군에서는 swe가 chat이 gate인 인스턴스(≤50 ms)에 얹혀
   살았는데, chat이 1대로 갇히자 얹힐 자리가 없어져 swe가 자기 gate(52~57 ms)
   인스턴스의 브림에서 돌았다. **격리가 E2E-예산 클래스에는 해롭다** — 이 부분은
   cap의 결함이 아니라 워크로드/예산 구조(EXP-104의 예산 사다리)가 cap으로
   증폭된 것이다.

**수정 (파라미터 0개 추가, 로직 제거)** — 바이너리 `08f9e9ce`, arm `fsv3capg`:
- 비례 정규화 삭제. 상한 = 자기 수요 ⌈λ/μ⌉ (바닥 1, hysteresis 유지).
- 차단에 수혜자 조건 추가: **다른 ready tier 중 gate 수가 자기 수요에 못 미치는
  클래스가 있을 때만** 상한이 구속력을 갖는다. 아무도 굶지 않으면 퍼짐 허용.
  (사용자 요구 철학: cap은 재분배 장치가 아니라 불공정 방지 guardrail — admitted
  ≥95 유지, "딱 그 정도 효과".)

**fsv3capg 판정 규칙 (실행 전 등록)**:
- G1 (기제): chat의 published limit이 부하 구간에서 1에 고정되지 않는다(원시 K≥2).
- G2: 전체 admitted ≥95, swe admitted가 대조군과 5점 이내.
- G3: chat 거절률이 대조군 +3점 이내.
- G4: dr offered가 대조군 +3점 이상을 유지한다(guardrail이 남기는 보호 —
  반복 1의 +8~12에서 줄어드는 것은 예상이고 허용).
- G5: 전체 offered가 대조군 ±3점 이내.
- 반증: guardrail이 결정을 바꾼 창이 5% 미만이면 이 워크로드에서 죽은 변수 —
  Discussion으로 강등. (chat-홍수 stress처럼 발화가 확실한 조건은 별도 실험.)

## 7. 본 체인 결과 (6/6, 2026-08-28 06:21 KST 종료) — 판정

요청 단위, swe는 E2E 30초, 분모별. 반복 폭이 전 arm에서 0.2~1.6점이라 아래 차이는
전부 읽을 수 있다.

| arm (2반복) | offered | admitted | 거절률 | goodput tok/s | chat off/rej | dr off/adm | swe off/adm |
|---|---|---|---|---|---|---|---|
| fsv3 대조군 | 76.4/78.2 | 97.4/97.5 | 21.1/19.3 | 11,148/11,249 | 85.7~88.6 / 10.4~13.3 | 61.9~65.6 / 93.6~95.3 | 46.9~52.4 / 86.6~91.0 |
| fsv3cap (구판) | 71.2/71.5 | 96.4/96.4 | 25.6/25.3 | 10,732/10,746 | 77.6~78.0 / 21.2~21.6 | **72.9~73.4** / 96.9~97.1 | 35.0~36.0 / **70.8~70.9** |
| **fsv3nofrc** | **81.3/80.1** | 96.9/97.4 | **15.7/17.3** | **11,748/11,614** | **90.1~92.6 / 6.3~8.9** | 67.2~67.3 / **99.9~100.0** | 47.1~50.5 / 74.4~79.0 |
| fsv3capnofrc | 73.4/73.3 | 97.0/96.9 | 23.7/23.8 | 11,061/11,038 | 79.4~79.5 / 19.7~19.8 | **75.1~75.4** / 99.5~99.6 | 39.2~40.1 / 72.4~72.6 |

**H2 (구판 cap): 기각 확정.** 전체 offered −5.8점(±3 밖), 두 반복 폭 0.3으로 재현.
원인은 §5.5의 진단(상시 정규화 → chat 상한 1 고정 + swe의 riding 상실).
dr 보호(+8~11)와 admitted 유지는 실재 — 형태가 아니라 정규화가 문제다.

**H4 (force→REJECT): 통과, 예상 초과.** 사전 예측은 "거절률 +2±1, offered −1.5
이내"였는데 실측은 **거절률 −4점, offered +2.9~+4.4점, goodput +4%** — 두 반복
비겹침. 카운터 분해: no_feasible(옛 force 인구의 명시 거절)가 2,539/2,500건
생겼는데 cannot_meet이 13,009/14,622로 대조군의 총거절보다 낮다 — **강제 배치가
동거 요청을 위반시키고 그 인스턴스의 feasibility를 무너뜨려 만들던 후속 거절
연쇄가, 원인(2.5천 건)을 끊자 그보다 큰 폭(약 4~6천 건)으로 사라졌다.** force의
동거-피해 연관(§0의 사전 측정)과 정합. → **채택 방향: 기본값 이동은 v0.4 절차로**
(드라이버가 기존 arm에 FS_FORCE=true를 명시한 뒤 기본값을 false로).

**H5 (상호작용):** capnofrc 실측 73.3~73.4 대 가법 예측 74.7 — 차이 −1.4, ±3 안.
근사 독립. 구판 cap은 무엇과 묶여도 자기 손실을 그대로 가져온다.

**H1:** 구판 cap의 기제 발화는 확인(blocked_feasible 19k+). 창별 5% 검사는
구판이 H2에서 기각되어 계산하지 않았고, guardrail판(§5.5의 G 규칙)에서 잰다.

## 8. guardrail판(fsv3capg) 결과 — G 판정 (2/2, 2026-08-28 09:08 KST)

| 2반복 | offered | admitted | 거절 | goodput | chat off/rej | dr off | swe off/adm |
|---|---|---|---|---|---|---|---|
| fsv3capg | **75.3/75.2** | 96.4/96.2 | 21.3/21.3 | 11,121/11,076 | 84.9~84.7 / 14.2~14.4 | **69.6~69.9** | 36.2~36.4 / **68.9~69.6** |

반복 폭 ≤0.2. 판정(§5.5의 사전 등록 규칙):

- **G1 통과** — chat 상한이 1~4를 수요 따라 움직인다(히스토그램 1:517 / 2:1447 /
  3:1164 / 4:538). 정규화 crush 소멸.
- **G2 절반 통과** — 전체 admitted 96.2~96.4 ≥95 ✓. **swe admitted 68.9~69.6은
  기각**(대조군 86.6~91.0, 5점 조건의 3~4배 이탈). 아래 §8.1.
- **G3 통과** — chat 거절 14.2~14.4, 대조군 평균 +2.5.
- **G4 통과** — dr offered +4.0~+8.0. 구판의 +8~11보다 작지만 "딱 그 정도 효과"
  라는 요구 그대로다.
- **G5 통과** — 전체 offered −1.2~−3.0 (±3 경계 안).
- **죽은 변수 아님** — cap이 excluded 카운터를 전진시킨 5초 창이 57.3~59.4%.
- 함대 구조: chat 없는 인스턴스-시간 48.3~50.1% (대조군 29~36, 구판 60.3) —
  guardrail이 구판의 절반 세기로 footprint를 눌렀다.

### 8.1 남은 것 하나 — swe admitted는 guardrail로도 안 돌아왔다 (구조적)

구판 진단(§5.5)의 "chat이 1대로 갇혀 riding 자리가 사라졌다"는 설명은
**절반만 맞았다**: chat을 2~3대로 돌려놓아도 swe admitted가 68.9~69.6으로 구판
(70.8~70.9)과 같다. swe의 blocked_feasible은 345~404건뿐이라 cap이 swe를 직접
막는 것도 아니다. 남는 설명: **cap이 만드는 chat-free 용량(48~50%) 자체가 swe에게
독이다** — swe가 자기 gate(nominal 57.7의 90% ≈ 52 ms) 인스턴스 브림에서 돌면
중앙값 요청(~577토큰)도 30초를 넘는다. dr에게는 그 용량이 정확히 보약이고(100 ms
면 충분) swe에게는 pace가 모자란다. **평균 출력로 유도한 nominal pace가 요청
절반에게 이미 부족한 클래스는, 더 빡빡한 클래스에 섞여야만 산다** — 워크로드/예산
구조의 성질이고 cap의 파라미터로 고칠 수 없다. 선택지: ⑴ swe 예산 40초(EXP-105가
측정, nominal 80 ms — 사용자가 보류), ⑵ E2E-형 클래스의 gate 유도를 평균이 아니라
분위수로(설계 변경, 두-곳-이동 함정 주의), ⑶ 수용하고 명시.

## 6. 이 실험이 재지 않는 것 (사전에 적는다)

- cap 상한 **안쪽**의 과잉 집중(EXP-97의 2대 몰림 → KV 천장)은 범위 밖.
- gate와 KV가 같은 토큰 재고를 읽는 시소(EXP-93 §9.5)는 cap이 있어도 성립 —
  cap은 용량 창출 수단이 아니다.
- 순수 홍수에서 특정 클래스에 함대를 남기는 것(운영자 가중치 w_c)은 기본값
  1로 고정, 이번에 쓸지 않는다.
- c_win(=3.0) 자체의 민감도 — H1이 살아남은 뒤의 후속.
