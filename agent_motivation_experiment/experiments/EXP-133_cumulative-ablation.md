# EXP-133 — 누적 ablation: 우리 것만 순서대로 벗겨 llm-d 쪽으로 수렴시킨다

실행 전에 씀. 2026-09-14 KST.

## 1. 왜 leave-one-out 이 틀린 모양이었나

EXP-131 이 여섯 요소를 하나씩 빼서 **셋이 0 이었다** — 잔액 조건 0, 보류 0, 클래스 선호는
한 시간 trace 에서 1.4~2.2. EXP-132 가 **왜 그렇게 작게 읽히는지를 직접 쟀다**: chat 을 하나도
들지 않은 (인스턴스, 시각) 쌍이 선호를 켜면 **2.1 / 4 대**, 꺼도 **1.8 / 4 대**다. 즉
**분리의 86% 를 판정 조건이 혼자 만든다.**

**두 메커니즘이 각자 같은 보호를 만들면, 하나씩 빼는 방식은 둘 다 0 으로 읽는다.** 둘을 같이
빼야 무너진다. 이 설계는 의도적으로 중복이 많으므로(격리를 게이트가 결과로 만들고, 선호가 그것을
강화하고, cap 이 상한을 씌운다) leave-one-out 이 **모든 요소를 구조적으로 과소평가한다.**

**그리고 회계가 안 맞는다.** 한 시간 trace 에서 FluidServe 78.7 대 llm-d 47.7 / 54.0 으로
**25~31 점** 차이인데, EXP-131·EXP-132 가 FluidServe 고유 메커니즘에 귀속시킨 것을 전부 더해도
**10 점이 안 된다.** 20 점 넘는 무언가가 고립되지 않았다.

## 2. 무엇을 벗기나 — 우리 것만, 공유하는 것은 건드리지 않는다

`related-works-review.md` §12.3 이 소스 대조로 정리한 **llm-d 와 남는 차이 여섯**을 기준으로
한다. **공유하는 것은 차별점이 아니므로 벗기지 않는다**: 인스턴스에 이미 있는 요청 중 가장
빡빡한 예산을 판정에 넣는 것(그들의 `running_request_tpot_slo_queue.go` 가 같은 값을 유지한다),
admission 으로 거절하는 것(`latency-slo-admitter`), prefix 선호
(`prefix-cache-affinity-filter`). 허용 속도 게이트의 여유 계수 0.90 도 벗기지 않는다 —
**그것은 파라미터이지 메커니즘이 아니다.**

| arm | 앞 단계에서 **하나** 더 뺀 것 | llm-d 와의 대응 |
|---|---|---|
| `cum0` | — (배포 설정 그대로) | |
| `cum1` | 클래스 선호 | 그들에게는 prefix 선호만 있다 |
| `cum2` | 보유(PEND) | §12.3 #6 — 도착 시 배치 아니면 거절 |
| `cum3` | 클래스별 인스턴스 상한 | 그들에게 없다 |
| `cum4` | KV flux 투영 | §12.3 #4 — KV 는 예측기의 입력 특징이지 제약이 아니다 |
| `cum5` | **도착 요청의 미래 디코드 선청구** (지평 1) | §12.3 #3 — `generatedTokenCounts[i] = 1` |

**`cum4` 는 `costOf` 를 건드리지 않는다** — `enableFlux` 는 `inflow` 와 `outflow` 만 0 으로
만든다(`fluidserve.go:1316`). 그래서 `cum5` 가 선청구를 깨끗하게 고립시킨다:
`costOf = promptTokens + min(horizonSteps, expectedToks)` 이므로 지평 1 은 프롬프트 + 1 토큰만
청구하고, 그것이 llm-d 예측기가 받는 것과 같다.

**각 arm 이 앞 arm 과 정확히 한 변수만 다른 것을 `set_arm` 을 불러 확인했다.**

## 3. `cum0` 은 대조군의 재측정이고, 그 자체가 유효성 검사다

`cum0` 은 배포 설정을 그대로 쓴다. **EXP-108 의 같은 셀(73.6~75.5, 2 반복) 안에 들어와야 하고,
안 들어오면 그 아래 단계를 하나도 읽을 수 없다** — 두 세션이 arm 말고 다른 것에서 다르다는
뜻이기 때문이다.

**이것은 EXP-108 과 중복이 아니다.** EXP-132 가 두 세션 사이에서 **preemption 이 2.1~2.6 배**
다른 것을 발견했고 원인을 찾지 못했다. `cum0` 은 **점수도 움직였는지**를 말해 주는 arm 이다.

## 4. 판정 규칙 — 실행 전에 정한다

조건당 **2 반복**, 정적 2100 rpm(35 req/s) 8 분, t75. 주 지표는 **모든 도착**, 옆에
admitted·거절률·goodput. 채점은 `FS_SWE_TBT_MS=75`(로더가 이제 강제한다).

| 차이 | 읽는 법 |
|---|---|
| \|Δ\| < 1.0 | 차이 없음 |
| 1.0 ≤ \|Δ\| < 3.0 | 판정하지 않는다 |
| \|Δ\| ≥ 3.0 | 실재하는 차이 |

**계단의 어느 칸에서 떨어지는지가 답이다.** 단계별 차이(`cum(n) − cum(n−1)`)를 그 요소의
**중복을 제거한 몫**으로 읽는다 — leave-one-out 값보다 클 것으로 예상하고, **그 둘의 차이가 곧
그 요소가 다른 요소와 얼마나 중복돼 있었는지**다.

### 4.1 사전 예측

- **`cum0` 은 73.6~75.5 안.** 벗어나면 아래를 읽지 않고 세션 차이부터 조사한다.
- **`cum1`(선호 제거)은 leave-one-out 과 같은 −3.3~−5.6** 이어야 한다. 첫 단계이므로 누적과
  leave-one-out 이 정의상 같다. **다르면 세션 효과이고 `cum0` 이 이미 그것을 말해 줄 것이다.**
- **`cum2`(보류) 와 `cum3`(cap) 은 leave-one-out 에서 0 이었다.** 누적에서도 0 이면 그
  요소들은 정말로 기여가 없는 것이고, 크면 **중복이 가려져 있었던 것**이다.
- **`cum4`(flux) 는 오히려 좋아질 것이다** — EXP-131 이 flux 를 끄면 정적에서 +5.5~+7.7
  좋아진다고 쟀다(`fsv3t75noflux` 81.3 / 81.0 대 대조군 73.6~75.5). 그러면 계단이 위로 꺾인다.
- **`cum5`(선청구 제거)가 가장 크게 떨어질 것이다.** EXP-131 의 지평 sweep 에서 h=1 이
  **34.5 / 35.0** 이었다. 다만 그때는 flux 가 켜져 있었으므로 같은 값이 나온다는 보장은 없다.

### 4.2 무엇이 이 실험을 반증하나

- **`cum5` 가 llm-d 의 47.7~54.0 근처로 내려오면** 25~31 점 차이가 이 메커니즘들로 설명된
  것이다.
- **안 내려오면**(예: `cum5` 가 여전히 70 점대) 남는 것은 **메커니즘 차이가 아니라 모델
  차이**다 — 우리는 프로파일에서 오프라인으로 적합한 해석적 step 시간 모델을 쓰고 그들은
  온라인으로 학습하는 예측기를 쓴다. **그 경우 논문은 그것을 함의로 두지 말고 그대로 적어야
  한다.**
- ⚠ **비교 대상이 다른 워크로드다.** llm-d 의 47.7 / 54.0 은 **한 시간 trace** 값이고 이 실험은
  **정적 35 req/s** 다. 계단의 절대값을 llm-d 의 한 시간 값과 직접 빼면 안 된다.
  llm-d 의 정적 35 req/s 값이 EXP-108 에 있으면 그것과 비교하고, 없으면 **계단의 모양만**
  읽고 llm-d 와의 비교는 한 시간에서 다시 한다.

## 5. 설정

| | |
|---|---|
| 함대 | Llama-3.1-70B, 4 인스턴스 × TP=2 |
| 스케줄러 | `063e1c85` |
| 조건 | 6 arm × 2 반복 = **12 조건**, 정적 2100 rpm, 8 분 |
| 예상 소요 | 약 **4 시간** |
| 드라이버 | `k8s/exp07/run_exp133_cumulative.sh` (`run_exp131_ablation.sh` 의 스냅샷) |

## 6. 결과

### 6.1 최종 (2 반복, 2026-09-14 01:37~05:35 KST)

여섯 arm 의 기동 줄을 반복마다 대조했고 의도한 설정이었다(단계마다 하나씩만 바뀐다).
`cum0` 이 **73.3~75.3** 으로 EXP-108 의 **73.6~75.5** 안에 들어와 **in-session 대조군이
재현됐다** — 세션 차이는 점수에 없다(preemption 에는 있다, §6.6).

| arm | 뺀 것 (누적) | 모든 도착 | **단계별 차이** | admitted | 거절률 | goodput |
|---|---|---|---|---|---|---|
| `cum0` | — (배포 설정) | 73.3~75.3 | | 98.5~99.2 | 22.0~23.4% | 12,990~13,259 |
| `cum1` | − 클래스 선호 | 71.3~71.4 | **−1.9 ~ −4.0** | 98.8~98.9 | 25.7~25.9% | 12,704~12,711 |
| `cum2` | − 보유(PEND) | 74.7~77.3 | **+3.3 ~ +6.1** | 99.2~99.3 | 20.7~22.1% | 12,886~13,384 |
| `cum3` | − 클래스별 인스턴스 상한 | **82.9~84.4** | **+5.6 ~ +9.7** | 99.1 | **13.5~14.9%** | 14,029~14,155 |
| `cum4` | − KV flux 투영 | 83.7~83.9 | −0.7 ~ +1.0 | 99.1 | 14.1~14.2% | 14,083~14,165 |
| `cum5` | − 미래 디코드 선청구 (지평 1) | 39.9~40.0 | −43.7 ~ −44.0 | 100.0 | **59.9~60.0%** | 6,055~6,166 |

그림·CSV: `results/aggregate_analysis/exp133_cumulative/exp133_cumulative_staircase.{png,csv}`.

### 6.2 ⚠ `cum5` 는 선청구를 고립시키지 못한다 — 실험 설계의 결함이다

**−44 를 "미래 KV 선청구의 몫"으로 읽으면 안 된다.** 지평이 `costOf` 에만 들어가는 것이
아니라 **속도 추정에도 들어간다**:

```go
// fluidserve_capacity.go:392  meanStepMs(..., horizon)
sp := prefillSteps(pendingPrefill, chunk)
k  := float64(horizon)
if sp > k { sp = k }
if sp <= 0 { return dec * corr }
return corr * ((k-sp)*dec + sp*(pre+dec-c0)) / k
```

그리고 판정 시점의 `newPending` 은 **도착 요청 자신의 프롬프트를 포함**하므로
(`fluidserve.go:2031`) **항상 0 보다 크다.** 따라서 `sp ≥ 1` 이 항상 참이고, 지평이 1 이면
`sp` 가 `k=1` 로 잘려 식이 `corr × (pre + dec − c0)` 가 된다 — **모든 후보의 예상 속도가
prefill 한 번의 비용이 되어** 게이트를 거의 항상 실패시킨다. 거절률 60% 가 그것이다.

**즉 지평은 한 파라미터에 메커니즘 셋이 들어 있다**: 방출을 공제하는 창(`expectedOutflow`),
도착 요청을 청구하는 창(`costOf`), 속도 추정의 prefill 비중의 분모(`meanStepMs`).
EXP-131 의 지평 곡선이 양 끝에서 나빠지고 25~50 에서 최대인 것이 이것으로 설명된다
(34.7 / 74.8 / **82.9** / **82.7** / 74.6 / 59.3 / 49.3) — **왼쪽 끝은 속도 추정의 포화,
오른쪽 끝은 청구의 증가다.**

→ **고쳤다 (§6.5).** `--fluidserve-charge-horizon` 을 추가해 청구 창을 계획 지평에서
분리했다. 기본값 0 은 계획 지평을 따르므로 **이전 측정의 뜻이 하나도 안 바뀐다.**

### 6.3 깨끗하게 읽히는 것 — 네 요소를 다 빼면 **좋아진다**

`cum0` → `cum4` 는 지평을 안 건드리므로 위 혼입이 없다. **74.3 → 83.8 (평균), 즉 약 +9.5 점
좋아진다.** 거절률이 22.7% → 14.1% 로 떨어지고 goodput 이 13,125 → 14,124 로 오른다.

| 요소 | 누적 단계 | leave-one-out (EXP-131) | 읽는 법 |
|---|---|---|---|
| 클래스 선호 | **−1.9 ~ −4.0** (번다) | −3.3 ~ −5.6 | **일치한다.** 첫 단계이므로 정의상 같아야 하고 실제로 같다 — **계단이 정상 작동한다는 확인** |
| 보유(PEND) | **+3.3 ~ +6.1** (손해다) | **0** | **leave-one-out 이 0 으로 읽던 것이 누적에서는 3~6 점 손해다.** 선호가 켜져 있으면 보유의 해가 안 보였다 |
| 클래스별 인스턴스 상한 | **+5.6 ~ +9.7** (손해다) | 안 쟀다 | **가장 큰 항이고 부호가 우리에게 불리하다.** EXP-134 가 배포 설정에서 이것 하나만 뺀 값을 잰다 |
| KV flux 투영 | −0.7 ~ +1.0 | −5.5 ~ −7.7 (끄면 좋아졌다) | 앞 단계들을 이미 뺀 뒤에는 **아무것도 안 한다.** 즉 flux 의 해도 다른 요소와 중복이었다 |

⚠ **누적이므로 조건부다.** `cum3` 의 +5.6~+9.7 은 *"선호와 보유가 이미 꺼진 상태에서 상한을
빼면"* 이다. EXP-107 은 선호가 **켜진** 상태에서 상한을 채택했으므로 배포 설정에서 상한 하나만
뺀 값은 다를 수 있다. **그래서 EXP-134 를 걸었다**(`nocapt75` = 배포 설정 − 상한, 2 반복).

⚠ **정적 35 req/s 한 셀이다.** EXP-107 이 상한을 채택한 근거는 **한 시간 trace 의 chat 이 많은
구간에서 deepresearch 가 8~12 점 오르는 재분배**였고 그것은 이 셀이 재는 양이 아니다.
**여기서의 손해는 채택을 한 시간 trace 에서 다시 보라는 근거이지, 상한을 버리라는 근거가
아니다.**

### 6.4 그래서 "성능이 어디서 나오는가"에 대한 현재 답

**우리가 채택한 네 메커니즘 중 점수를 버는 것은 클래스 선호 하나(2~4 점)뿐이고, 보유와
인스턴스 상한은 이 셀에서 각각 3~6 점과 6~10 점을 잃게 하고 있다.** 지평은 메커니즘 셋이
섞여 있어 아직 값을 말할 수 없고, §6.5 의 플래그가 그것을 가능하게 한다.

**llm-d 와의 25~31 점 차이는 아직 설명되지 않았다.** 남은 후보 둘:

1. **미래 디코드 선청구** — `--fluidserve-charge-horizon=1` 을 계획 지평 100 과 함께 주면
   이것만 끈 조건이 된다. **이것이 다음에 재야 할 값이다.**
2. **모델 품질** — 우리는 프로파일에서 오프라인으로 적합한 해석적 step 시간 모델을 쓰고
   그들은 온라인으로 학습하는 예측기를 쓴다. 메커니즘 ablation 으로는 안 나온다.

### 6.5 `--fluidserve-charge-horizon` 을 추가했다 (바이너리 `12faf17f`, **아직 배포 안 함**)

`costOf` 가 쓰는 청구 창을 계획 지평에서 분리한다. **기본값 0 은 계획 지평을 따르므로
2026-09-14 이전의 모든 측정이 그대로 재현된다.** `1` 로 주면 **선청구만** 꺼지고 속도 추정과
방출 공제는 그대로다 — `cum5` 가 하려던 것이 이것이다.

- `cmd/config/config.go` 의 플래그 설명에 **왜 필요한지**(지평이 세 곳에 들어간다는 것)를
  적었다.
- 기동 줄에 `chargehorizon=%d` 를 추가했으므로 `set_scheduler_profiling.py` 의 검증이 되읽는다.
- `FS_CHARGE_HORIZON` 을 `FLUIDSERVE_ABLATIONS` 에 등록했다.
- `go vet` 통과, `go test ./pkg/scheduler/policy/` 통과.
  ⚠ 처음 돌렸을 때 `TestExecuteMigrationsSuccess` 가 5.00 초 정확히에서 실패했는데,
  **실험이 도는 중이라 호스트가 바빠서 걸린 타임아웃**이었다. 단독 3 회와 전체 재실행에서
  전부 통과한다. **변경과 무관하다.**
- **배포하지 않았다** — 실험이 도는 동안 `bin/` 을 덮어쓰지 않는다는 규칙 때문이고,
  `bin/scheduler-exp07` 은 `063e1c85` 그대로다.

### 6.6 부산물 — preemption 의 세션 효과가 확정됐다

`cum0` 은 배포 설정 그대로이므로 EXP-132 가 열어 둔 질문에 답한다. 같은 셀에서 완료
1,000 건당 preemption 이 **EXP-108(08-31) 16.7 / 15.2, EXP-131(09-12) 35.5~44.1,
`cum0`(09-14) 45.1** 이다. **설정이 같고 점수가 같은데 preemption 만 2.7~3.0 배다.**
원인은 정책이 아니라 환경이고, 가장 유력한 것은 엔진 인자의 `--api-server-count 4`
(여덟 인스턴스 작업 때 2026-09-04 추가, 함대를 되돌린 뒤에도 남음). 자세한 것은
`EXP-132_*.md` §6.1.

## 7. 남은 것

1. **EXP-134** — 배포 설정에서 인스턴스 상한 하나만 뺀 값(2 반복). 05:40 KST 에 걸었다.
2. **선청구만 끈 조건** — `FS_CHARGE_HORIZON=1`, 계획 지평 100. 바이너리 `12faf17f` 배포 필요.
3. **보유와 상한을 한 시간 trace 에서 다시 본다.** 이 셀의 손해가 EXP-107 이 채택 근거로 삼은
   재분배와 상충하는지 확인해야 한다.
4. 계단의 순서를 뒤집은 판(상한 → 보유 → 선호)을 돌리면 각 요소의 중복 구조가 더 보인다.
   지금 판은 선호를 먼저 뺐으므로 나머지 셋의 값이 "선호가 없는 세계"의 값이다.
