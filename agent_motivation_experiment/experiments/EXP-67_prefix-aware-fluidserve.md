# EXP-67 — FluidServe가 prefix cache 재사용을 보면 어떻게 되는가

**상태**: 구현·단위 테스트 완료(`e594d8a`), **2026-08-08 02:00 KST에 실행을 걸어 둠**
(`/home/nxclab/tools/exp67_after_exp66.sh`, EXP-66 rep 2가 끝나면 자동 시작, 감시 동반).
**설계 정본**: [`ms_dev/notes/fluidserve-prefix.md`](../../../ms_dev/notes/fluidserve-prefix.md).
이 파일은 **실행 전에 적는 판정 규칙**과 조건 목록이다.

## 1. 왜

EXP-66에서 llm-d가 45 req/s 이상 전 구간에서 앞섰고(55 req/s offered 84.3 대 66.4),
원인을 축 여덟 개로 검사한 결과가 **클래스 분리가 아니라 prefix cache 재사용**이었다
(`llmd-baseline.md` §9.11). 70 req/s에서 엔진이 보고한 hit rate가 93.5% 대 75.1%이고,
실제로 계산한 prefill이 4,585 대 12,274 tok/s이며, 그 차이가 decode로 갔다
(20,661 대 16,562 tok/s).

우리 쪽 원인은 **task 단위 지역성**이다 — llm-d는 같은 task의 요청 99.9%를 같은 엔진으로
보내고 우리는 57.8~62.5%다. 클래스 선호는 클래스 수준의 지역성만 주고 그 아래에서 캐시가
깨진다.

## 2. 무엇을 바꿨나

도착 프롬프트의 prefill 비용이 `promptTokens × prefillFractionOf()`(fleet 스칼라 하나)에서
`(promptTokens − hitTokens(요청, 인스턴스)) × κ`로 바뀐다. **"몇 토큰을 계산해야 하는가"만
교체하고, "그 토큰이 엔진 시간으로 얼마인가"는 지금의 측정 경로(엔진의 대기 prefill,
측정된 duty cycle, 청크 모델)가 그대로 답한다.** KV 발자국은 할인하지 않는다.

κ는 지금의 `prefillFraction`인데 **분모가 바뀌어 뜻이 바뀐다**: `computed / Σ promptTokens`
(할인 그 자체)에서 `computed / Σ 예측 charge`(**그 예측의 오차**)로. 범위도 [0.02, 1.0]에서
[0.25, 4.0]으로 넓힌다 — 잔차는 1을 넘을 수 있어야 하고, 1을 넘는 경우가 바로 **색인이 엔진에
없는 hit을 주장한 경우**다.

## 3. ⚠ 먼저 정해야 하는 것 — 워크로드를 고칠 것인가

**아래 권고대로 진행했다 — 지금 워크로드로 EXP-67을 걸었고, 워크로드 수정은 적용하지 않았다.**
고치는 패치는 `/home/nxclab/tools/staging/fix_worker_prompt_duplication.py`에 준비돼 있고
`--check`로 적용 가능함을 확인했다. **워크로드를 고칠지, 고친다면 어느 arm을 다시 잴지는
사용자 결정이 필요하다**(§8.6에 비용). 2026-08-08에 부하 생성기의 결함을 찾았다:
`_worker_main`의 워커별 데이터셋 분할이 `isinstance(dataset, list)`로 갈라지는데 mixed
워크로드는 dict를 돌려주어 **분할이 통째로 건너뛰어졌고, 워커 12개가 같은 프롬프트 열을 그대로
보냈다.** 모든 프롬프트가 정확히 12번씩 나갔고 그만큼 prefix hit rate가 부풀려져 있다
(`fluidserve-prefix.md` §8).

| | 고치지 않고 EXP-67 | 먼저 고치고 EXP-67 |
|---|---|---|
| 비교 대상 | EXP-53/57/66과 나란히 놓인다 | **다섯 arm 전부 재측정 필요**(arm당 약 6시간) |
| 측정하는 조건 | prefix 재사용이 실제보다 큰 조건. **prefix affinity로 라우팅하는 정책에 유리** | 현실적인 조건 |
| 우리 변경의 이득 | 과대평가될 수 있다 | 제대로 잰다 |

**권고**: 두 단계로 나눈다. **EXP-67은 지금 워크로드로** 돌려 변경이 의도대로 동작하는지부터
확인하고(H1·H2가 그것을 잰다), 동작이 확인되면 **EXP-68에서 고친 워크로드로 FluidServe와
llm-d 둘만** 재서 방향을 본다. 이유: 워크로드와 정책을 같은 단계에서 바꾸면 둘을 구별할 수
없고, 이것은 이 저장소가 이미 겪은 실패다(프로파일과 워크로드를 따로 갱신하지 않아 생긴
CLAUDE.md 함정 A의 항목).

## 4. arm

| arm | 플래그 | 무엇을 가른다 | 이번에 도는가 |
|---|---|---|---|
| `fluidserve` | (기본, prefix off) | 대조군 | **돈다** |
| `fspfx` | `--fluidserve-prefix-aware=true` | 이 변경 전체 | **돈다** |
| `fspfx-nocal` | `+ --fluidserve-prefix-calibration=false` | κ가 무엇을 하는지 | 드라이버에 정의만 해 두고 **이번에는 안 돈다** |

**실제로 건 것: 2 arm × 3 rate(2700/3300/4200 rpm = 45/55/70 req/s) × 2 반복 = 12조건,
약 4.5시간.**

- **여덟 rate가 아니라 셋인 이유**: 45·55·70이 EXP-66에서 llm-d가 앞선 구간이고(+1.3, +17.9,
  +14.2), 동시에 feasibility가 구속력을 갖는 구간이다. 설계 §5대로 이 변경은 그 구간에서만
  작동할 수 있으므로, 15~35 req/s는 세 정책 전부 100점이라 읽을 것이 없다.
- **대조군을 같은 세션에 두는 이유**: EXP-53의 FluidServe를 재사용하면 절반 값이지만,
  **그 arm의 반복 간 편차가 60 req/s에서 11.7점**으로 이번에 읽으려는 효과보다 크다.
  같은 세션의 대조군은 세션 오프셋을 공유하므로 차분에서 상쇄된다.
- **⚠ 반복 2회가 모자랄 수 있다** — prefix 색인은 이력 의존 상태라 같은 설정의 두 run이 더
  갈라진다. **반복 간 편차를 먼저 읽고, 읽으려는 차이보다 크면 반복을 늘린다.**

## 5. 판정 규칙 — 실행 전에 적는다

| # | 예상 | 무엇이 나오면 틀린 것인가 | 어디서 읽나 |
|---|---|---|---|
| H1 | 엔진별 prefix hit rate가 75.1% → **85% 이상** | 안 오르면 신호가 결정에 도달하지 못한 것 | `server_metrics/engine_*.jsonl`의 `vllm:prefix_cache_hits_total / queries_total` |
| H2 | task당 유효 엔진 수가 2.08 → **1.6 이하** | 안 내려가면 순위가 여전히 클래스 항에 지배되는 것 | `analysis_scripts/request_level/separation_measures.py`, task 단위 |
| H3 | 45~70 req/s에서 offered 달성률이 **반복 간 편차보다 크게** 오른다 | 편차 안이면 이득 없음 | `exp53_compare.py` |
| H4 | κ가 **1.0 ± 0.25** 안에 머문다 | 1.5를 넘으면 축출을 못 따라가는 것 → 색인 용량이나 TTL을 손봐야 한다 | `scheduler_fluidserve_prefill_fraction` |
| H5 | `infeasible_total{reason}`가 gate/incumbents에서 **memory 쪽으로** 이동 | 안 움직이면 charge가 결정을 안 바꾼 것 | `scheduler_fluidserve_infeasible_total` |
| H6 | 클래스별 유효 인스턴스 수가 **0.3 이상 움직이지 않는다** | 움직이면 클래스 분리도 같이 바뀐 것이라 두 축을 못 가른다 | 같은 스크립트, 클래스 단위 |

**H1과 H2가 핵심이고, 둘 다 실패할 수 있다는 것을 미리 안다.** 이유는 설계 문서 §5에 적었다:
`sortCandidates`의 점수가 `w·share + (1−w)·room`이고 기본 w=1.0인데 **prefix는 그 어느 항에도
안 들어간다.** feasibility 판정과 TTFT 판정에만 들어가므로, 캐시가 있는 쪽과 없는 쪽이 둘 다
feasible이면 클래스 점유율이 이긴다. 즉 **지역성은 feasibility가 구속력을 가질 때만 생긴다.**

→ 둘 다 실패하면 다음 선택지는 `score`에 세 번째 항을 넣는 것인데, **그 순간 "예측을 정확하게
만든다"가 "새 목적함수를 넣는다"로 바뀌어 논지의 성격이 달라진다.** 이 실험에는 넣지 않고,
결과를 보고 따로 결정한다.

## 6. 사전 점검 — 조건을 걸기 전에

1. **배포된 바이너리가 새 플래그를 안다.** `--fluidserve-prefix-aware`가 `--help`에 있고
   기동 줄에 `prefix=true, prefixcalib=..., prefixblock=16, prefixcap=500000`이 찍히는지.
   ⚠ 드라이버가 플래그를 고정하면 그 바이너리에만 있어야 한다(CLAUDE.md 함정 B).
2. **대조군 arm이 정말 대조군인가.** `set_scheduler_profiling.py`가 이번 호출에서 설정하지
   않은 ablation을 제거하는지 확인하고, 기동 줄을 되읽어 `prefix=false`인지 본다.
3. **한 시간 trace의 서로 다른 프롬프트 수를 센다.** `--fluidserve-prefix-capacity` 기본값
   500,000이 충분한지는 8분 조건(0.30 M 블록)이 아니라 한 시간 조건으로 정해야 한다.
4. **감시를 같이 건다.** `/home/nxclab/tools/watch_experiment.sh` (CLAUDE.md 함정 B).
5. `kubectl get nodes` + `df -h`로 DiskPressure를 본다.

### ⚠ EXP-53과 다른 배경 하나

llm-d의 스택 다섯 파드(router/EPP, 학습 서버, 예측 서버 셋)가 **엔진과 같은 노드 nxc13에 떠
있는 채로** EXP-67이 돈다. EXP-53·EXP-57이 돌 때는 없던 것이다. 트래픽이 안 가므로 유휴에
가깝겠지만 **크기를 재지 않았다**(이 클러스터에 metrics-server가 없다).

**EXP-67의 판정에는 영향이 없다** — 대조군 `fluidserve`가 같은 세션에서 같은 배경으로 돌고,
읽는 것은 두 arm의 차이다. **영향이 있는 것은 EXP-67의 절대값을 EXP-53·EXP-66과 나란히 놓을
때**이고, 그때는 이 차이를 밝힌다. 내리지 않은 이유는 llm-d를 다시 재려면 다시 세워야 하고
그 비용이 이 배경 차이보다 크기 때문이다.

## 6.5 유효성 확인 둘 (2026-08-08 03:30, 사용자 질문에 답해 확인)

### 출력 길이 예측이 oracle로 들어가고 있지 않은가 → **아니다**

세 경로를 전부 확인했다.

| 확인한 것 | 결과 |
|---|---|
| 스케줄러가 받는 `types.SchedulingRequest` | **출력 길이 필드가 아예 없다.** `PromptNumTokens`, `PromptTokenIds`, `TtftSloMs`, `TpotSloMs`, `Id`, `Model` 뿐이다 |
| `requestBudget(tier)` (fluidserve_registry.go:673) | **인자가 tier 하나**다. `prof.expectedRemaining(0)` — 그 클래스 분포의 기댓값이고 요청과 무관하다 |
| 디코드 중 `remaining` | `expectedRemaining(j)`로 갱신되는데, `j`는 **엔진의 step 카운터에서 추론한** 지금까지 만든 토큰 수다. 관측이지 정답이 아니다 |
| 클라이언트가 보내는 `max_tokens` | **4,096으로 고정**(`run_config.json`). run 전체·모든 arm 공통 상수다 |
| transcript의 `recorded_output_tokens` | task dict에 들어 있지만 **분석에만 쓰이고 전송되지 않는다** |

즉 길이 예측은 처음부터 끝까지 **클래스 조건부**다. 클래스 자체는 클라이언트가 선언한
(ttft, tbt) 쌍에서 오는데, 그것은 실제 클라이언트도 선언하는 것이므로 oracle이 아니다.

### 길이 예측 프로파일이 수정된 상태로 돌고 있지 않은가 → **아니다**

EXP-63이 `scale_class_profile.py`로 deepresearch를 ×0.5~×2.0으로 바꿔 쓰던 파일이라 확인이
필요했다. 저장소 파일과 **클러스터가 마운트한 ConfigMap이 정확히 일치**한다:

| 클래스 | mean |
|---|---|
| swe | 494.0 |
| chat | 428.0 |
| deepresearch | **984.8** |

셋 다 2026-08-02 재적합(`435f3d3`)의 값이고 스케일된 값이 아니다(×0.5면 492.4, ×2.0이면
1,969.7). 파일 mtime이 08-07 15:05 = EXP-63 종료 시각인 것은 **그 체인이 마지막에 원본을
되돌렸기 때문**이고, `435f3d3` 이후 이 파일을 바꾼 커밋은 없다.

## 7. 결과 (2026-08-08 05:38 KST, 12조건 전부 완료)

`results/aggregate_analysis/exp67/`. 대조군과 처리군이 **같은 세션에 붙어 있고 플래그가 하나만
다르다**(보관된 배포 spec에서 확인: 처리군 `--fluidserve-prefix-aware=true`, 대조군은 그
플래그가 제거되어 컴파일 기본값 false).

### 7.1 요약 — 작동한다. 부하가 낮을수록 이득이 크다

등가중 offered 달성률과 token goodput, 두 반복 평균(괄호는 폭):

| req/s | 대조 `fluidserve` | goodput | 처리 `fspfx` | goodput | Δ |
|---|---|---|---|---|---|
| 45 | 85.2 (3.9) | 20,564 | **96.2** (7.3) | 21,934 | **+11.0** |
| 55 | 64.7 (0.8) | 19,952 | **72.5** (1.1) | 22,292 | **+7.8** |
| 70 | 49.5 (1.3) | 19,880 | **52.1** (0.8) | 21,216 | **+2.6** |

**세 rate 전부 반복 폭보다 큰 차이다**(45만 폭이 커서 7.3 대 11.0으로 여유가 작다).

| req/s | 거절 건수 대조→처리 | swe 달성률 | chat 달성률 |
|---|---|---|---|
| 45 | 1,428 → 285 (**−80%**) | 65.2 → **90.2** | 92.5 → 98.4 |
| 55 | 6,213 → 4,344 (−30%) | 35.4 → **42.8** | 70.3 → 81.0 |
| 70 | 13,870 → 10,814 (−22%) | 15.9 → **21.6** | 53.3 → 56.6 |

**이득의 대부분이 swe다** — 프롬프트가 가장 길고(평균 6,472 토큰) 공통 접두사가 가장 큰
클래스이므로, 인스턴스별로 정확히 물리는 것이 가장 크게 달라지는 곳이다.

### 7.2 llm-d 대비 — goodput은 따라잡았고 달성률은 절반만 좁혔다

| req/s | fspfx | llm-d | 달성률 | fspfx goodput | llm-d goodput | goodput |
|---|---|---|---|---|---|---|
| 45 | **96.2** | 92.5 | **+3.7** | **21,934** | 20,988 | **+4.5%** |
| 55 | 72.5 | **82.3** | −9.8 | **22,292** | 21,980 | **+1.4%** |
| 70 | 52.1 | **66.0** | −13.9 | 21,216 | 21,345 | −0.6% |

- **45에서는 우리가 앞선다** — 달성률과 goodput 둘 다. ⚠ 우리 반복 폭이 7.3이라 확립은 약하다.
- **55에서 격차가 절반이 됐다** — EXP-53 대비 17.6점이던 것이 9.8점이다. 그리고 **goodput은
  이미 우리가 앞선다.**
- **70에서는 거의 안 좁혀졌다**(16.5 → 13.9).
- **goodput만 보면 세 rate 전부 llm-d와 같거나 앞선다.**

### 7.3 사전 등록한 가설 판정

| # | 예상 | 결과 |
|---|---|---|
| **H1** | 엔진 prefix hit 75.1% → 85% 이상 | **통과.** 45에서 84.9→89.8, 55에서 80.7→86.7, **70에서 76.6→86.4**. 부하가 높을수록 이득이 크다 |
| **H2** | task당 유효 엔진 2.08 → 1.6 이하 | **실패 — 그런데 뜻이 다르다. §7.4** |
| **H3** | 반복 폭보다 큰 달성률 상승 | **통과.** 세 rate 전부 |
| **H4** | κ가 1.0 ± 0.25 | **실패. 0.53~1.14로 떠돈다. §7.5** |
| **H5** | infeasible 사유가 memory 쪽으로 이동 | 미확인 (다음에 본다) |
| **H6** | 클래스별 유효 인스턴스 수가 0.3 이상 안 움직임 | 미확인 |

### 7.4 H2가 실패한 이유는 정책이 아니라 워크로드다

task당 유효 엔진 수가 **두 arm에서 소수점 둘째 자리까지 같다** — 55 req/s에서 2.33 대 2.33,
70에서 2.07 대 2.07.

원인을 쟀다: **같은 프롬프트의 12개 사본이 2.1초 안에**(중앙값) 도착하고, 연속 사본 간격이
**0.11초**이며 **99.9%가 1초 이내**다(§8의 워커 분할 결함). 같은 프롬프트 12개가 사실상 동시에
몰려오면 feasibility 판정이 그것들을 흩을 수밖에 없다.

→ **H2 실패는 "우리 정책이 지역성을 못 만든다"의 증거가 아니라 "이 워크로드에서는 물어볼 수
없다"의 증거다.** 워크로드를 고치기 전에는 그 질문에 답이 없다. 이것이
`ms_dev/notes/workload-decision.md`에서 워크로드 수정의 가장 강한 근거가 됐다.

**지역성이 안 바뀌었는데 hit rate는 올랐다**는 것은 모순이 아니다. 거절이 20~80% 줄어 같은
프롬프트가 엔진에 더 많이 도달하고, 12개 묶음 안에서 먼저 놓인 것이 색인을 채우면 나머지에
대해 그 엔진이 가장 싸지므로 **묶음 안에서** 더 모인다. 전 구간 평균 지표는 그 효과를 못 본다.

### 7.5 H4가 실패했고, 그것이 구현 결함을 드러냈다

κ가 여섯 조건 전부에서 1.0~1.14로 시작해 0.53~0.90까지 내려가고 **끝까지 안정되지 않는다.**

원인: **κ가 자기 자신에 되먹임되고 있었다.** `prefillChargeFor`가 `(raw − hit) × κ`를
돌려주고 `commit`이 그 κ 곱해진 값을 누적하며 `notePrefill`이 그것으로 나눈다. 평형이
**κ² = computed/raw**이므로 κ가 참 잔차의 **제곱근**으로 수렴한다. r < 1이면 √r > r이므로
**엔진이 실제로 하는 일의 1/√r 배를 물린다** — 실측 κ에서 역산하면 참 잔차가 0.30~0.64이고
**1.6~1.9배 과다 청구**다.

→ **이번 결과는 그만큼 보수적인 상태에서 나온 값이다.** 고쳤고
(`prefillChargeFor`가 보정 전/후를 따로 돌려주고 보정은 보정 전으로 나눈다),
`TestCalibrationConvergesToTheResidual`이 두 루프를 고정점까지 몰아 확인한다 — 고친 쪽 0.400,
깨진 쪽 0.632(=√0.4). **EXP-67b가 그 수정본을 55 req/s에서 2반복 잰다.**

### 7.6 기각한 가설 하나

κ가 끝까지 내려가고 색인이 계속 커져서(8분에 59k → 144k 블록) "8분이 정상 상태에 못 미쳐
이득이 저평가된다"고 의심했다. **조건 안에서 두 arm의 격차를 90초 창으로 재보니 시간에 따라
커지지 않는다**(대체로 일정한 5~10점). **이득은 처음부터 있고 κ의 표류는 별개의 보정
과도상태다.** 기각.

---

## 8. EXP-67b — κ 되먹임을 고치면 어떻게 되나 (2026-08-08 06:09 완료)

§7.5가 찾은 결함을 고친 바이너리(`5a572dc2`, 파드에서 `md5sum /proc/1/exe`로 검증)로
**55 req/s 2반복**만 다시 쟀다. 55를 고른 이유는 llm-d가 아직 9.8점 앞서고 처리군의 반복 폭이
가장 작아(1.1) 추가 이득이 가장 잘 읽히는 rate이기 때문이다. arm 이름은 `fspfxb`
(SESSION_SUFFIX=b로 분리했고 플래그는 `fspfx`와 완전히 같다 — **다른 것은 바이너리뿐**).

| arm | offered | goodput | 거절 |
|---|---|---|---|
| 대조 `fluidserve` | 64.7 | 19,952 | 6,213 |
| `fspfx` (κ 되먹임 있던 판) | **72.5** | 22,292 | 4,345 |
| `fspfxb` (κ 고침) | **72.4** | 22,530 | 3,891 |

### 결과: 음성이다

**κ는 의도대로 내려갔다** — 조건 안 최소가 되먹임 판의 0.53~0.59에서 **0.40~0.43**으로
움직였다. 즉 수정이 먹혔다. **그런데 달성률이 72.4 대 72.5로 안 움직인다.**
goodput +1.1%, 거절 −10%는 반복 폭 안이다.

### 왜인지도 확인했다 — 병목이 옮겨갔다

`scheduler_fluidserve_infeasible_total`을 사유별로 보면:

| arm | 총 판정 실패 | gate | incumbents | memory |
|---|---|---|---|---|
| 대조 `fluidserve` | 1,513,412 | **95.6%** | 4.4% | **0%** |
| `fspfx` | 1,277,474 | 94.2% | 5.7% | **0%** |
| `fspfxb` | 1,614,844 | **92.5%** | 7.5% | **0%** |

**H5의 예상이 틀렸다.** "prefill 항이 느슨해지면 memory 쪽으로 이동한다"고 적었는데
**memory는 애초에 한 번도 안 걸린다** — 이 워크로드는 KV 점유가 22~36%라 용량 근처에 안 간다.
**병목은 gate**이고, gate는 `meanAfter > gateAllowance × utilisation`이다.

→ **prefill 비용을 정확하게 만들고 나니 더는 prefill이 `meanAfter`를 gate 위로 밀어올리는
주항이 아니다. 디코드 부하가 그 자리를 차지한다.** 그래서 prefill을 더 깎아도 안 움직인다.

### 그래서 다음은 gate다

55·70에서 llm-d에 지는 부분이 swe인데(42.8 대 80.2), swe가 gate에 막히는 것은
**인스턴스에 chat이 하나라도 있으면 그 인스턴스의 허용 속도가 chat 예산인 50 ms가 되고,
swe는 자기 예산이 훨씬 느슨한데도 그 50 ms로 판정받기 때문**이다. 이것은 이미 탐색한 축이다
— `gateSlack`(EXP-52), `ownBudgetGate`(EXP-46·50). **워크로드를 고친 뒤 그 축을 다시 보는 것이
지금 시점에서 가장 큰 남은 여지로 보인다.**
