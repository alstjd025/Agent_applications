# EXP-113 — 다섯 컨트롤플레인, Qwen2.5-72B, 한 시간 mix-shift trace

**2026-09-03 ~ 04.** 두 번째 모델에서 정적 sweep 없이 **한 시간 실험만** 한다(사용자 결정).
묻는 것은 **"용량 대비 같은 자리에서 정책 순위가 유지되는가"** 다.

## 1. 도착률을 왜 낮췄는가

원본 trace `dyn60_shift_m2Am1B_b1045` 는 Llama-3.1-70B 의 용량에 맞춰 만들어진 것이다.
같은 파일을 Qwen 에 그대로 쓰면 다른 실험이 된다:

| | Llama (무릎 28.4) | Qwen (무릎 18.3) |
|---|---|---|
| 평균 부하 | 27.4 req/s = **0.96×** | 27.4 req/s = **1.50×** |
| 무릎 위에 있는 시간 | **45%** (27/60 분) | **78%** (47/60 분) |
| 최대 부하 | 1.59× | 2.46× |

Llama 에서 이 trace 는 **용량 언저리를 오가며 절반쯤을 과부하로 보내도록** 설계됐고, 그
전환에서 정책이 어떻게 반응하는지가 이 실험이 보는 것이다. 78% 가 과부하이면 과소부하
구간이 사라지고 모든 arm 이 내내 거절하거나 백로그를 쌓는 한 종류의 상태만 재게 된다.

→ **`traces/scale_trace.py --factor 0.644 --down-method thin`** 으로 도착을 솎아냈다.
배수는 EXP-112 가 잰 90% 교차점의 비(18.3/28.4)다. **시간 축과 버스트 위치는 그대로**
두고 도착만 줄인다(늘리면 93 분이 되어 믹스 전환 일정까지 바뀐다).

결과(`_q064`): 도착률 **7.0~29.6 req/s**, 평균 **17.3 = 0.95× 무릎**, 최대 1.62×, 무릎 위
**44%** — Llama 에서의 원본과 같은 자리다. 클래스 믹스도 보존됐다(chat 65.4 → 65.2 /
deepresearch 21.5 → 21.6 / swe 13.0 → 13.2%). 63,437 건(원본 98,649).

⚠ **두 열은 위치만 공유한다.** 요청 수가 63.4k 대 98.0k 이므로 **개수는 비교할 수 없고
비율과 초당 값만** 비교한다.

## 2. 결과 (반복 1)

**기존 채점** (`FS_SWE_TBT_MS=75`)

| arm | 요청 | 거절률 | offered | admitted | goodput | 창(초) | Llama offered (2반복) |
|---|---|---|---|---|---|---|---|
| **FluidServe** | 63,163 | 18.5% | **78.6** | 96.6 | **11,876** | 3,580 | 78.7 / 78.0 |
| vLLM router | 63,091 | 0.0% | 53.0 | 53.0 | 2,687 | **3,729** | 36.9 / 37.7 |
| PolyServe | 63,206 | 38.1% | 50.6 | 82.0 | 8,523 | 3,584 | 31.4 / 30.8 |
| llm-d | 63,168 | 44.4% | 50.4 | 91.0 | 8,747 | 3,580 | 47.7 / 54.0 |
| Llumnix SLO | 63,172 | 43.9% | 19.5 | 35.0 | 2,848 | 3,581 | 20.5 / 20.9 |

**누적 마감 규칙** (토큰 *i* 의 마감 = TTFT_SLO + *i*×TBT_SLO, 요청은 토큰 95% 이상)

| arm | offered | admitted | 거절률 | goodput | throughput | n |
|---|---|---|---|---|---|---|
| **FluidServe** | **81.3** | 99.9 | 18.6% | **12,266** | 12,269 | 62,825 |
| PolyServe | 60.3 | 97.6 | 38.2% | 9,612 | 9,684 | 62,953 |
| vLLM router | 55.2 | 55.2 | 0.0% | 2,907 | 6,483 | **27,105** |
| llm-d | 54.3 | 98.1 | 44.6% | 9,254 | 9,316 | 62,880 |
| Llumnix SLO | 29.6 | 53.0 | 44.3% | 7,860 | 10,398 | 62,645 |

**FluidServe 가 두 채점 모두 1 위**이고, 2 위와의 격차는 offered +25.6 / +21.0 점,
goodput +3,129(1.36 배) / +2,654(1.28 배)다. **Llama 에서의 순위가 유지된다.**

## 3. 같이 읽어야 하는 것 넷

**① vLLM router 의 분모는 다른 arm 과 같은 도착 스트림이 아니다.** 거절을 하지 않아 부하
생성기가 밀렸다. 분 단위 도착 수(원본 `metrics.csv`, 분석 필터 전후 동일):

| 분 | 50 | 51 | 52 | 53 | 54 | 55 |
|---|---|---|---|---|---|---|
| FluidServe | 1,511 | 1,734 | 1,625 | 1,780 | 1,694 | 1,617 |
| PolyServe | 1,505 | 1,736 | 1,628 | 1,772 | 1,700 | 1,621 |
| Llumnix SLO | 1,501 | 1,743 | 1,624 | 1,775 | 1,697 | 1,617 |
| **vLLM router** | 1,305 | **704** | **700** | **551** | **702** | 1,575 |

창도 149 초 길고, 누적 규칙의 `n` 이 27,105 로 다른 arm 의 절반이 안 된다(토큰 이벤트가
남은 요청이 그만큼 적다). **이 arm 의 offered 를 인용할 때는 이것을 같이 적는다.**

**② 두 채점이 2 위를 다르게 지목한다.** 기존 규칙은 vLLM router(53.0), 누적 규칙은
PolyServe(60.3)를 2 위로 놓는다. Llumnix SLO 는 admitted 가 35.0 → 53.0 으로 크게 오르는데,
누적 마감이 **초반의 빠름으로 후반의 느림을 상쇄해 주기 때문**이다. **규칙에 따라 바뀌는
것은 2·3 위이고 1 위는 바뀌지 않는다.**

**③ Llama 대비 변화가 arm 마다 다르다.** FluidServe(78.6 대 78.7/78.0)와 Llumnix
SLO(19.5 대 20.5/20.9)는 그대로, llm-d(50.4)는 두 반복(47.7/54.0) 사이, **PolyServe(+19.8)와
vLLM router(+15.7)만 크게 올랐다.** 오른 두 arm 의 공통점은 이번에 프로파일·표가 Qwen
실측에서 다시 만들어졌다는 것이지만(PolyServe 의 tier 별 토큰 수가
`25:494,50:428,100:985` → `25:1006,50:519,100:1826`), **반복 하나로 원인을 확정하지
않는다.** 반복 2 를 돌리는 이유의 하나가 이것이다.

**④ 인스턴스 부하가 고르지 않은 것은 고장이 아니다.** 실행 중에 GPU 두 장의 이용률이 0 인
것을 보고 인스턴스가 죽은 줄 알았으나, 넷 다 응답했고 그 인스턴스도 이미 110 만 토큰을
만든 뒤였다. 엔진별 구성이 원인이다 — 8002 는 **chat 89.2%** 를 24,263 건 들고 preemption
0 건, 8001 은 **deepresearch 42.8% + swe 25.1%** 를 6,170 건만 들고 preemption 392 건.
요청 수는 3.9 배 차이지만 **엔진별 admitted 달성률은 95.9~97.7% 로 고르다.** 클래스 분리가
작동한 결과이고, goodput 도 Llama(11,465~11,521)보다 높다(11,876).

## 4. 그림

`analysis_scripts/redraw_hour_trace_exp113.sh` → `results/aggregate_analysis/exp113_hour_qwen/`.
**두 세트로 나눴다** — 한 캔버스에 섞으면 읽는 사람이 두 질문을 구분할 수 없다.

- **[A] 다섯 정책, 같은 축소 hour** — 표준 8 패널, 엔진 계층 시계열, 엔진별 요청 구성,
  정책 나란히, 클래스별 goodput, arm 별 엔진 패널, 클래스 분리 지표.
- **[B] `models/`** — FluidServe 하나를 두 모델에서. **이 세트만 의도적으로 서로 다른
  trace 를 그린다**(각자 자기 용량의 0.95 배).

## 5. 이번에 고친 계측·도구 결함

1. **`exp41_dynamic_timeline.py` 의 패널 A 가 "all N arms see the same trace" 를 조건 없이
   찍고 있었다.** 세트 B 에서는 거짓이다. 이제 **그려진 곡선에서 판정**한다. 처음 넣은
   판은 존재하지 않는 변수를 참조해 **항상 "같다"로 떨어지는 무늬만 수정**이었고, 다시
   고쳤다. 그리고 그 검사가 세트 A 에서 참을 냈을 때 오탐인 줄 알고 완화하려 했으나
   **재 보니 오탐이 아니었다**(③ 의 vLLM router). 문구를 `REALISED ARRIVALS DIFFER` 로
   바꿨다 — trace 파일이 아니라 실제로 보내진 도착이 다르다.
2. **`traces/scale_trace.py` 가 솎아낸 뒤 다시 첫 도착을 0 으로 맞춘다.** 첫 도착이 솎여
   나가면 살아남은 도착이 전부 앞으로 밀리는데 phase·segment 라벨은 행을 따라가므로
   warmup 과 믹스 전환 일정이 시계와 어긋난다. `thin` 에서만 생기고 고쳤다. ⚠ 처음
   이 결함의 증거로 든 "경계가 60.05 → 59.95 로 옮겨졌다"는 **오진이었다** — 그것은
   모든 축소 trace 가 받는 입력 재기준 0.1197 s 와, 원본 경계 행이 솎여 나가 다음 measure
   행이 첫 행이 된 것이다. 이번 배수에서는 첫 도착이 살아남아 문제의 뺄셈이 0 이었다.
3. **러너가 워크로드의 `class_plan_file` 과 `--trace-file` 이 다르면 시작을 거부한다**(각
   도착의 클래스는 trace 의 열이므로 같은 파일이어야 한다). 축소 trace 용 설정 둘을 만들고
   드라이버가 변형까지 보도록 고쳤다.
4. **러너 템플릿이 trace 옆의 `.plan.json` 을 읽는다.** 축소판 계획을 축소된 CSV 에서 다시
   측정해 만들었다. 처음엔 warmup 이 0 으로 나왔는데 trace 는 그 구간을 `warmup_m2` 로,
   plan 은 `warmup` 으로 부르기 때문이었다. 지금은 **양쪽에 없는 구간이 있으면 중단**한다.
5. **llm-d 드라이버에 엔드포인트 갱신이 빠져 있었다.** llm-d 는 `configmap/llmd-endpoints`
   의 **파드 IP** 로 라우팅하는데 드라이버가 조건마다 엔진을 재시작한다. 확인 시점에
   ConfigMap 은 10.42.0.26, 파드는 10.42.0.19 였다 — **존재하지 않는 주소로 라우팅할
   뻔했다.** `run_exp113_llmd.sh` 가 재시작 직후 갱신하고 실패하면 중단한다.
6. **`build_request_engine_map.py` 의 인자 형태를 틀려** Qwen run 이 엔진별 그림과 분리
   지표에서 조용히 빠져 있었다(그 뒤 `IndexError`). 고쳤고 100% 귀속된다.

## 6. 실행에서 걸린 것

- **정상으로 돌던 run 을 내가 죽였다.** 드라이버를 "명령줄에 스크립트 이름이 있는
  프로세스"로 셌는데 **bash 가 fork 한 서브셸이 같은 명령줄을 갖는다.** 하나짜리 실행이
  둘로 읽혔고 측정 4 분 시점에 죽였다. **드라이버는 세션·프로세스 그룹 리더만 센다**
  (PPID 1 또는 PGID==PID). 부분 run 둘은 `excluded_runs.tsv` 에 있다.
- **감시를 하네스가 추적하지 않는 형태로 띄우면 알림이 오지 않는다.** EXP-111·112 의
  감시는 종료를 제대로 감지해 파일에 썼는데 나에게 전달되지 않았다.
- vLLM router 는 83 분 걸렸다(다른 arm 70 분). 거절하지 않아 백로그가 쌓이고 유한한
  drain 을 다 쓴다. **미리 95 분을 이상 판정선으로 정해 두었고** 그 안이었다.

## 7. 반복 2 — 두 반복이 맞는다 (2026-09-04 08:41 종료)

**기존 채점, 반복 1 / 반복 2**

| arm | 거절률 | offered | admitted | goodput | offered 편차 |
|---|---|---|---|---|---|
| **FluidServe** | 18.5 / 18.6 | **78.6 / 78.7** | 96.6 / 96.8 | 11,876 / 11,849 | **0.1** |
| vLLM router | 0.0 / 0.0 | 53.0 / 54.1 | 53.0 / 54.1 | 2,687 / 2,615 | 1.1 |
| PolyServe | 38.1 / 37.8 | 50.6 / 50.0 | 82.0 / 80.5 | 8,523 / 8,451 | 0.6 |
| llm-d | 44.4 / 42.5 | 50.4 / 52.9 | 91.0 / 92.2 | 8,747 / 9,015 | **2.5** |
| Llumnix SLO | 43.9 / 44.2 | 19.5 / 19.7 | 35.0 / 35.5 | 2,848 / 2,893 | 0.2 |

**누적 마감 규칙, 반복 1 / 반복 2 (offered)**: FluidServe 81.3 / 81.2, PolyServe 60.3 / 60.6,
vLLM router 55.2 / 58.1, llm-d 54.3 / 56.4, Llumnix SLO 29.6 / 29.7.

**FluidServe 가 두 채점·두 반복 모두 1 위**이고 2 위와 24.5~25.6 점(기존) / 20.6~23.1 점
(누적) 차이다. **그 격차가 가장 큰 반복 간 편차(llm-d 2.5 점)의 열 배**이므로 순위는 편차로
설명되지 않는다.

**§3-③ 이 닫혔다.** PolyServe(50.6/50.0)와 vLLM router(53.0/54.1)가 Llama 대비 크게 오른 것은
**우연한 run 이 아니다** — 두 반복이 각각 0.6 점과 1.1 점 안에서 재현된다. 다만 **왜 올랐는지는
여전히 미검증이다**: 후보(Qwen 실측으로 다시 만든 프로파일과 PolyServe 의 tier 별 토큰 수 표)를
확인하려면 옛 표를 그대로 둔 ablation 이 필요하다.

**llm-d 가 이 세트에서 가장 불안정하다** — offered 2.5 점, goodput 268 tok/s. Llama 에서도
47.7/54.0 으로 6.3 점 벌어졌고, EXP-110 에서 낮은 도착률의 outlier 를 조사한 것도 이 arm 이다.
**이 arm 의 값을 인용할 때는 반복 두 개를 다 적는다.**

**그림**: 두 반복을 각 arm 의 짙은/옅은 색으로 **둘 다 그린다**(평균으로 지우지 않는다).
`exp113_five_control_planes_..._two_repeats_each_..._timeline.png`.
⚠ 같은 디렉토리에 **반복 1 만 그린 옛 파일이 남아 있다**(제목이 파일명이 되므로 이름이 다르다).
파일을 고를 때 `ls -t | head -1` 로 최신을 잡는다.

## 8. 반복 2 의 실행

02:30 KST 시작, 08:41 종료, 5/5. 체인은 **재개 가능**하다(병합된 결과가 있는 arm 은
건너뛴다). 세션 밖에서 도는 supervisor 가 체인을 지켰고 재기동은 시작 시점의 1 회뿐이다.

⚠ **그 supervisor 를 띄우면서 재기동 루프를 만들었다.** "체인이 살아 있나" 를 **PPID==1** 로
물었는데 `setsid` 는 가능하면 fork 없이 exec 하므로 자식이 init 으로 재부모화되지 않고 띄운
쪽이 부모로 남는다. 살아 있는 체인을 죽은 것으로 읽고 60 초마다 다시 띄워 **3 분 만에 한
함대에 체인이 둘** 붙었다. 명령줄 자체로 판정하도록 고쳤다. 수습 중에 **살아남은 체인이
24 초 전에 만든 Job 을 내가 지워** 그 체인이 없는 Job 을 기다리며 멈춘 것도 되돌렸다.
**측정 시작 전이라 오염된 데이터는 없다.** 규칙은 CLAUDE.md 함정 F 에 넣었다 — 이름으로 세면
많게, PPID 로 세면 적게 나오고 이번 실험에서 두 방향 모두 한 번씩 틀렸다.
