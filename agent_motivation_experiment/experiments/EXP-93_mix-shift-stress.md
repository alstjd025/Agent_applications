# EXP-93 — 믹스가 균등 구간을 통과하는 stress trace: 믹스 이동은 시스템이 고르지 않은 용량 변화다

**작성 2026-08-23, 실행 전.** 사용자 지시로 "(나) 믹스가 균등 구간을 통과하는 한 시간 trace"를
건다. 비교 arm은 FluidServe v0.2(`fspfx`)와 llm-d(`llmdslo`), 2반복, 총 4 run 약 5시간.

## 1. 왜 이 실험이 필요한가

심사 반문 중 **"workload distribution shift에 robust한가"**에 대한 지금 답은
`paper_evaluation_2026-08/13_robustness_map.md`이고, 그 근거의 동적 부분은 배포된 한 시간
trace(`dyn60_short_m123_b1045`) 하나다. **그 trace에서 chat이 요청의 66.7~93.0% 사이에서만
움직이고 균등(33.3%) 영역에 한 번도 가지 않는다.** 그래서 지금 쓸 수 있는 문장은
"chat 다수 안에서 믹스가 이동해도 견딘다"까지이고, 리뷰어가 묻는 distribution shift의 폭을
덮지 못한다.

이 실험이 새로 묻는 것은 그 폭만이 아니다. **믹스가 움직이면 도착률이 그대로여도 시스템이
초당 감당할 수 있는 요청 수가 바뀐다.** 우리 워크로드에서 클래스별 평균 입력 길이가
chat 674 / deepresearch 4,055 / swe 6,812 토큰으로 열 배 차이가 나므로, 요청 개수 비율이
바뀌면 **요청 하나의 무게가 바뀌고 무릎(90% 달성률을 유지하는 최대 도착률)이 같이 움직인다.**
도착률은 그 변화를 전혀 알려주지 않는다. **이것이 라우팅 계층이 적응해야 하고 고정 배정이
못 하는 것의 가장 직접적인 형태이며, 아직 아무도 재지 않았다.**

## 2. 무엇을 바꿨나 — 믹스 일정 하나뿐이다

새 trace `traces/dynamic/canonical/dyn60_shift_m2Am1B_b1045.csv`.
생성 인자가 배포된 한 시간 trace와 **seed(20260726)·도착률 대역(10~45 req/s)·Azure 원본
구간·구간 길이(900초)·warmup(60초)까지 전부 같고 `--mix-schedule`만 다르다.**

**검증 완료 (실행 전)**: 두 CSV의 `arrival_s` 열이 99,242행 **전부 동일**하고, `class` 열만
46,632행(47.0%)이 다르다.

```
m123 (배포된 것)  m1 → m2 → m3 → m1     chat 76.9 → 93.0 → 66.7 → 76.9 %
shift (이번 것)   m2 → A  → m1 → B      chat 93.0 → 33.3 → 76.9 → 60.0 %
```

**단조 하강이 아니라 큰 하강 → 큰 상승 → 중간 하강**으로 잡았다. 단조로 만들면 "믹스가
무거워진 것"과 "시간이 흘러 밀린 요청이 쌓인 것"이 구분되지 않고, 되돌아오는 구간이 없으면
**회복하는지**를 못 본다.

## 3. 구간별 상태 — 이 표가 실험의 설계다

무릎 추정은 **m1의 측정된 무릎 28.0 req/s(EXP-80, 8 도착률 2반복)를 요청당 입력 토큰 수에
반비례로 환산한 것**이다. 균등 믹스에 대해 이 방법이 12.1을 주는데, EXP-92의 두 점(15 req/s
83.3~85.0, 20 req/s 71.3~73.7 offered)에서 외삽한 12.5와 맞으므로 환산법이 대략 맞는다.
**m2·B의 무릎은 측정된 적이 없는 추정치다.**

| 구간 | 믹스 (요청 개수 기준) | 입력 토큰 기준 | 요청당 입력 | 도착률 p5/중앙/p95 | 무릎 추정 | 구간 중 무릎 위 |
|---|---|---|---|---|---|---|
| s0 | m2 — chat 93.0 / dr 4.7 / swe 2.3% | 64.4 / 19.4 / 16.2% | 974 | 10.7 / **23.6** / 41.6 | ≈48 | **0%** |
| s1 | **A — 33.3 / 33.3 / 33.3%** | 5.8 / 35.1 / 59.0% | **3,847** | 14.0 / **24.7** / 38.7 | **≈12** | **100%** |
| s2 | m1 — 76.9 / 15.4 / 7.7% | 31.1 / 37.5 / 31.4% | 1,666 | 13.9 / 27.4 / 38.7 | **28.0** (측정) | 48% |
| s3 | B — 60.0 / 30.0 / 10.0% | 17.6 / 52.8 / 29.6% | 2,302 | 16.7 / 35.8 / 44.6 | ≈20 | 88% |

> **s0과 s1의 도착률 중앙값이 23.6 대 24.7로 4.7% 차이인데 무릎 대비 위치는 0% 대 100%로
> 정반대다.** 같은 도착률이 한 구간에서는 무릎의 절반이고 다음 구간에서는 두 배다. **이 짝이
> 이 실험의 핵심 측정이고, 같은 run 안에 있으므로 세션 효과가 원리적으로 들어올 수 없다.**

⚠ **"균등"한 것은 요청 개수이지 토큰이 아니다.** s1에서 입력 토큰의 59.0%가 swe이고 chat은
5.8%다. 요청당 입력이 974 → 3,847 토큰으로 **3.95배**가 된다. 배포된 trace의 같은 값은
1,666 → 974 → 2,413 → 1,666으로 범위가 2.48배이므로, **이 trace는 요청 무게의 이동 폭을
2.48배에서 3.95배로 넓힌다.**

## 4. 가설과 반증 조건 — 실행 전에 확정한다

주 지표는 **요청 단위 offered 달성률**(모든 도착이 분모, 거절은 위반)이고, 그 옆에 admitted,
거절률, token goodput을 항상 같이 낸다.

### H1 (핵심, run 안에서 닫힌다) — 같은 도착률에서 구간 사이 차이가 구간 안 도착률 효과보다 크다

s0과 s1의 **겹치는 도착률 구간(약 14~39 req/s)에서 같은 도착률 bin끼리** FluidServe의 달성률을
비교한다.

- **확인**: 겹치는 bin 전부에서 s1이 s0보다 낮고, **평균 차이가 20점 이상**.
- **반증**: 평균 차이가 **8점 미만**이거나 부호가 bin마다 뒤집힌다. 그러면 "믹스가 용량을
  바꾼다"는 주장을 이 데이터로 못 한다.
- 8~20점이면 구별은 되나 약하다고 적는다.

**세션 효과가 들어올 수 없다** — 두 구간이 같은 run의 서로 다른 15분이다.

### H2 — 전환 직후의 비용이 llm-d에서 더 크다

각 구간 경계 앞 3분과 뒤 3분의 달성률 차이를 arm별로 낸다. 경계는 셋
(m2→A 하강, A→m1 상승, m1→B 하강).

- **확인**: **m2→A 경계에서 llm-d의 하락이 FluidServe보다 5점 이상 크다.**
- **반증**: FluidServe의 하락이 llm-d보다 5점 이상 크다.
- ⚠ 전환 3분 창에는 도착률 자신의 변화도 섞이는데, **생성기가 구간 경계를 도착률 봉우리와
  일부러 어긋나게 두므로** 두 arm이 같은 도착률 변화를 겪는다. arm 사이 차분에서 상쇄된다.

### H3 (부차, 세션을 건너간다) — 넓힌 믹스 이동이 우리 우위를 줄이지 않는다

배포된 한 시간 trace에서 EXP-71이 잰 값은 FluidServe 81.0(폭 1.5) 대 llm-d 56.7(폭 1.8),
**격차 +24.3**이다.

- **확인**: 이 trace에서도 격차 **20점 이상**.
- **반증**: 격차 **12점 이하** — 믹스 이동 폭을 넓히면 우리 우위가 절반이 된다는 뜻이고,
  robustness 주장을 그만큼 좁혀 적어야 한다.
- ⚠ **이것은 세션을 건너가는 비교다.** EXP-71은 2026-08-09, 바이너리 `a96dac12`,
  게이트웨이 `GOMAXPROCS` 미설정. 지금은 `89d9593b`(추가된 플래그의 기본값이 false라 동작
  동일)와 `GOMAXPROCS=16`(EXP-82가 달성률·거절률·goodput은 반복 폭 안에서 안 움직인다고
  확인). **그래도 H1·H2를 주 근거로 쓰고 H3은 보조로 쓴다.**

### 4.4 실행 전에 잰 기준점 — 배포된 trace에서 같은 스크립트로 재면 얼마인가

**새 run이 나오기 전에** 분석 스크립트(`analysis_scripts/request_level/exp93_mix_shift.py`)를
**배포된 한 시간 trace의 EXP-91 4 run에 먼저 돌렸다.** 목적은 두 가지였다 — 코드 경로를
실제 데이터로 검증하는 것과, H1이 재는 양이 **지금 trace에서 이미 얼마인지**를 사후가 아니라
사전에 못박아 두는 것이다.

**검증**: 스크립트가 EXP-91의 기록값을 소수 첫째 자리까지 재현한다(offered 84.1 / 82.4 /
82.5 / 82.8, 반복 폭 1.6과 0.4). 구간별 실현 chat 비중도 목표와 0.1%p 이내로 일치해
구간 태깅이 맞다.

**기준점**: 배포된 trace에서 가장 chat이 많은 구간(m2, 93.0%)과 균등에 가장 가까운
구간(m3, 66.7%)을 **같은 도착률 bin에서** 비교하면

| req/s bin | 15~20 | 20~25 | 25~30 | 30~35 | 35~40 | **평균** |
|---|---|---|---|---|---|---|
| `fspfx` 차이 | +3.1 | +7.4 | +16.3 | +28.5 | +31.7 | **+17.4** |
| `fsnoaff` 차이 | +3.4 | +10.0 | +22.0 | +31.2 | +32.2 | **+19.8** |

**즉 기제는 배포된 trace에서도 이미 보이고, 도착률과 함께 커진다**(저부하에서 3점, 고부하에서
32점). 이 trace의 믹스 거리는 chat 93.0 → 66.7%(26.3%p)이고 **새 trace는 93.0 → 33.3%
(59.7%p)로 2.3배**다.

> **그래서 H1의 문턱 20점은 그대로 두되, 실제로 쓸 문장은 절대값이 아니라 두 trace의 비교다:**
> "믹스 거리를 2.3배로 넓히면 같은 도착률에서의 구간 간 차이가 +17.4에서 X로 간다."
> **두 trace의 도착 시각이 완전히 동일하고 같은 스크립트로 채점되므로 이 비교가 성립한다.**
> ⚠ 다만 EXP-91은 다른 세션이므로 **arm의 절대 점수를 나란히 놓을 때는 세션 효과를 적는다.**
> 구간 간 차이는 각 run 안에서 만들어지므로 그 영향을 받지 않는다.

## 5. 유효성 검사 — 하나라도 실패하면 그 부분은 판정하지 않는다

| | 무엇 | 문턱 |
|---|---|---|
| V1 | 구간별 실현 비율이 목표와 일치 | ✅ **이미 통과**: 0.9303 / 0.3333 / 0.7692 / 0.6000, 목표와 0.1%p 이내 |
| V2 | 도착 시각이 배포 trace와 동일 | ✅ **이미 통과**: 99,242행 전부 일치 |
| V3 | 두 arm의 도착 수가 서로 1% 이내 | `agent != "job_summary"`로 센다(`grace_cut` 포함) |
| V4 | 요청→엔진 조인 성공률 ≥95% | 한 시간 PolyServe에서 86.4%까지 떨어진 적이 있다 |
| V5 | 어느 구간에서 두 arm이 **둘 다** offered 20점 미만이면 그 구간에서 arm 차이를 읽지 않는다 | 붕괴 대 붕괴 비교 방지 |
| V6 | 스케줄러 기동 줄의 정책·플래그가 arm 정의와 일치 | 드라이버의 abort가 검사한다 |

## 6. 알려진 한계 — 고치지 않고 기록한다

1. **s1에서 swe의 prefix 재사용이 부풀려진다.** transcript가 1,500건인데 s1에서 swe가 도착의
   3분의 1이라 **사본 간격이 약 2.7분**이고, 스케줄러 prefix 색인의 보유 시간(약 2.8분)
   안이다. m1 구간에서는 9.1분이라 안전하다. **두 arm이 같은 trace를 보지만 prefix 재사용을
   쓰는 방법이 다르므로(우리는 인스턴스별 prefill 청구, llm-d는 cache-aware 점수), s1의
   수치를 prefix 동작의 깨끗한 비교로 읽으면 안 된다.** transcript 확대는 별도 미결 과제이고
   지금 하면 더 나쁜 문제(13,218건 판은 구조적 prefix 공유가 98.9%)로 바뀐다.
2. **믹스 이동 폭이 Azure가 뒷받침하는 범위를 넘는다.** Azure 2024의 conv/code 비 이동이
   4.71배이고 배포 trace의 swe 입력 토큰 비중 이동이 4.07배인데, 이 trace는 요청 무게 기준
   3.95배이면서 chat 비중을 93%에서 33%로 옮긴다. **논문에서 "현실적 trace"가 아니라
   "의도적으로 범위를 넓힌 stress test"라고 이름을 붙여야 한다.**
3. **llm-d에 예열 주행을 주지 않는다** — EXP-71과 같다. trace의 도착률이 움직여서 예열할
   고정 도착률이 없다. trace 자신의 60초 warmup 구간에서, 그리고 측정 중에 예측기가 학습한다.
4. m2와 B의 무릎은 **추정치**다(측정된 것은 m1의 28.0뿐).

## 7. 실행

**trace를 다시 만드는 명령** (`.gitignore`가 `*.csv`를 거르므로 CSV는 저장소에 안 들어간다.
seed가 고정이라 이 명령이 같은 파일을 만든다):

```bash
python3 traces/dynamic/build_dynamic_mix_trace.py \
  --out traces/dynamic/canonical/dyn60_shift_m2Am1B_b1045 \
  --rate-min 10 --rate-max 45 \
  --mix-schedule m2,A,m1,B --warmup-mix m2 \
  --seed 20260726 --source-day0 0 --source-days 4 --source-start-hour 6 \
  --duration-min 60 --bin-sec 1.0 --warmup-sec 60
```

**조건**:

```bash
# FluidServe v0.2
ARMS=fspfx SESSION_PREFIX_BASE=exp93 ./run_exp93_shift.sh shift 1
# llm-d
./run_exp93_llmd_shift.sh full exp93r1_llmdslo_shift
```
연쇄는 `/home/nxclab/tools/exp93.sh`(반복 둘, 반복 1이 끝난 뒤 반복 2).
드라이버 둘 다 스냅샷이고 `bash -n` 통과. 배포 바이너리 `89d9593b`, 엔진 stock,
migration off, 게이트웨이 `GOMAXPROCS=16`.

## 8. 결과 (2026-08-23 18:33 KST, 4/4조건)

전체 표는 `results/aggregate_analysis/exp93/report.md`. 채점은
`analysis_scripts/request_level/exp93_mix_shift.py`, 주 지표는 요청 단위 offered 달성률.

### 8.0 유효성 검사 전부 통과

실현 chat 비중이 네 run 모두 93.0 / 33.3~33.4 / 76.9 / 60.0%로 목표와 0.1%p 이내 →
**구간 태깅이 맞다.** 도착 수 98,231~98,255로 arm 간 편차 0.02%(V3 통과). shard 전부 병합,
`ABORT`/`FAILED` 0건. **어느 구간도 두 arm이 함께 붕괴하지 않았다**(V5 통과) — 가장 낮은 칸이
llm-d의 s3_B 27.5로 문턱 20 위다.

### 8.1 H1 확인 — 같은 도착률에서 믹스가 용량을 정한다. 그리고 우리가 덜 잃는다

s0(chat 93.0%)과 s1(균등)을 **같은 run 안에서** 도착률 bin으로 짝지은 결과. 경계에 걸친
창은 양쪽 모두에서 제외했다.

| req/s bin | 10~15 | 15~20 | 20~25 | 25~30 | 30~35 | 35~40 | **평균** |
|---|---|---|---|---|---|---|---|
| **FluidServe** | +17.0 | +24.1 | +30.7 | +40.0 | +48.3 | +47.6 | **+34.6** |
| **llm-d** | +33.0 | +45.2 | +54.9 | +58.4 | +54.4 | +46.9 | **+48.8** |

**두 arm 모두 여섯 bin 전부 같은 부호이고 확인 문턱 20점을 넘는다 → 확인.**

> **도착률이 전혀 안 바뀌었는데 요청 개수 비율이 바뀌었다는 것만으로 달성률이 34.6점(우리)
> 내지 48.8점(llm-d) 내려간다.** 부하를 보는 어떤 신호도 이 변화를 예고하지 않는다 —
> 두 구간의 도착률 중앙값이 23.6과 24.7 req/s로 4.7% 차이다.

**효과가 믹스 거리에 거의 비례한다.** 실행 전에 배포된 trace(chat 93.0 → 66.7%, 거리 26.3%p)에
같은 스크립트를 돌려 잰 기준점이 다섯 bin 평균 **+17.4**였다. 이 trace(거리 59.7%p, **2.27배**)의
**같은 다섯 bin** 평균이 **+38.1로 2.19배**다.

**⚠ 이 방향의 confound는 결과를 약하게 만드는 쪽이다.** s1에서 swe transcript 사본 간격이
약 2.7분이라 prefix 재사용이 부풀려져 있고, 그것은 s1의 성적을 **실제보다 좋게** 만든다.
따라서 참값의 격차는 위 값보다 **크면 컸지 작지 않다.**

### 8.2 H2 확인 — 전환에서 llm-d가 두 배 넘게 떨어지고, 우리만 완전히 회복한다

| 경계 | 믹스 단계 | FluidServe | llm-d |
|---|---|---|---|
| s0→s1 (큰 하강) | chat 93.0 → 33.3% | 99.0 → 75.5 (**−23.5**) | 95.7 → 42.5 (**−53.2**) |
| s1→s2 (상승) | chat 33.3 → 76.9% | 71.8 → **98.5** (+26.6) | 50.2 → 83.4 (+33.1) |
| s2→s3 (하강) | chat 76.9 → 60.0% | 97.0 → 82.1 (−14.9) | 76.8 → 47.2 (−29.6) |

**사전 등록한 비교(큰 하강에서 llm-d 하락이 5점 이상 큰가)의 답은 차이 +29.7 → 확인.**
세 경계 전부에서 llm-d 쪽이 더 크게 움직인다(−53.2 대 −23.5, +33.1 대 +26.6, −29.6 대 −14.9).

**회복이 갈린다**: 균등 구간을 지나 chat 다수로 돌아오면 **FluidServe는 98.5로 하강 전
99.0에 사실상 복귀**하는데 **llm-d는 83.4로 하강 전 95.7에 12.3점 못 미친다.**
→ 쓸 수 있는 문장: "믹스가 되돌아와도 llm-d는 되돌아오지 않는다."

### 8.3 H3 확인 — 이동 폭을 2.3배로 넓혀도 우위가 줄지 않는다

| arm | offered | admitted | 거절률 | goodput tok/s |
|---|---|---|---|---|
| **FluidServe** | **75.5** (75.3~75.7) | 93.2 | 18.9% | 10,640 |
| llm-d | **50.3** (49.8~50.7) | 92.6 | 45.6% | 8,062 |

**격차 +25.2 → 확인**(문턱 20). 배포된 trace의 같은 격차가 EXP-71에서 **+24.3**이었다.

⚠ **두 값을 나란히 놓는 것은 세션을 건너간다**(EXP-71은 2026-08-09, 바이너리 `a96dac12`,
`GOMAXPROCS` 미설정). 그래서 **주 근거는 H1·H2이고 이것은 보조다.** 그 단서를 달고 읽으면:
넓힌 이동이 **두 arm 모두에게 비슷한 절대 비용**을 물린다(FluidServe 83.3 → 75.5로 −7.8,
llm-d 56.7 → 50.3으로 −6.4). **격차가 유지되는 것이지 우리가 이동에 면역인 것이 아니다.**

⚠ **llm-d의 admitted 열은 오류 문자열 분류에 달려 있다**(그 arm 거절의 96~100%가
`KV_THRESHOLD`, 즉 HTTP 500 경로). **헤드라인으로 쓴 offered는 그 분류에 둔감하다.**

### 8.4 논문에 쓸 문장과, 쓰면 안 되는 것

**쓸 수 있는 것**:
- "요청 개수 기준 클래스 비율이 chat 93%에서 균등으로 옮겨가면, **도착률이 그대로여도** 같은
  도착률에서의 달성률이 우리 34.6점 llm-d 48.8점 내려간다. 부하 신호에는 이 변화가 없다."
- "그 손실의 크기가 믹스 이동 거리에 거의 비례한다(거리 2.27배 → 손실 2.19배)."
- "전환 직후 손실이 llm-d의 절반 이하이고(−23.5 대 −53.2), 믹스가 되돌아왔을 때 우리만
  원래 수준으로 복귀한다(98.5 대 83.4, 하강 전 99.0과 95.7)."
- "이동 폭을 2.3배로 넓혀도 llm-d 대비 우위는 +24.3에서 +25.2로 유지된다."

**쓰면 안 되는 것**:
- **"현실적 워크로드"** — 이동 폭이 Azure 근거(conv/code 비 4.71배)를 넘는다. **stress test**로
  이름 붙인다.
- **"우리는 믹스 이동에 영향받지 않는다"** — 한 시간 총계로 7.8점 잃는다.
- **s1 구간의 prefix 동작 비교** — 그 구간은 swe 재사용이 부풀려져 있다(8.1의 ⚠).
- **s0의 무릎 약 48 req/s와 s3의 약 20 req/s를 측정값으로** — 환산 추정치다(측정된 것은 m1의 28.0).

---

## 9. 이어붙인 조건 — 클래스 선호를 끄고 같은 trace (2026-08-23 23:30 KST 실행 전 작성)

`fsnoaff` 2반복, 같은 trace·같은 도착 시각. 비교 대상은 §8의 `fspfx` 2 run이다.

### 9.1 왜 이 칸인가 — 앞의 세 축과 조건이 다르다

클래스 선호는 세 축에서 총계 무효과였다: 정적 m1(25·35에서 +2.8/+3.8, **45에서 −7.1**),
한 시간 m1(EXP-91 **+0.7**), 정적 m5(EXP-92 +1.0/+0.6). **유일한 예외인 정적 45 req/s에는
기제가 있다** — 선호가 클래스를 적은 인스턴스에 모으고 그 인스턴스가 KV 한계에 먼저 닿아서
메모리 사유 탈락이 18.8% 대 3.4%가 된다.

**EXP-91이 +0.7을 낸 이유는 그 trace가 무릎 위에 있는 시간이 약 절반이라 +3.8과 −7.1이
상쇄되기 때문이다.** §3의 구간표가 보이듯 **이 trace는 s1이 구간의 100%, s3가 88%가 무릎
위**여서, **교차점 위에서 보내는 시간이 훨씬 많은 첫 동적 조건**이다.

> **두 결과가 다 정보다.** 음수면 평평한 세 결과가 **"선호의 부호는 함대가 교차점 위에서
> 보내는 시간 비율이 정한다"**는 규칙으로 바뀐다. 또 평평하면 **그 교차점 설명이 죽는다** —
> 부하 위치와 무관하게 동적 총계가 선호에 둔감하다는 뜻이다.

### 9.2 가설과 반증 조건

**H4 (총계)**: 이 trace에서 **선호를 끈 쪽이 3점 이상 높다**(즉 선호가 해롭다).
- **반증**: 켠 쪽이 3점 이상 높다 — 또는 **차이가 3점 미만**이면 구별 불가로 보고하고
  **교차점 설명을 철회한다.**
- 문턱 3점의 근거: §8의 `fspfx` 반복 폭이 **0.4점**(75.3~75.7)이고 EXP-91의 폭이 1.6과
  0.4다. 3점은 그 위다.

**H5 (믹스 이동의 대가가 선호 때문인가)**: §8.1의 구간 간 격차(s0 대 s1, 짝지은 도착률)가
**선호를 끄면 5점 이상 작아진다.** 근거: 균등 구간은 세 클래스가 똑같이 있어 **모을 것이 가장
많은 구간**이므로 집중이 비용의 일부라면 여기서 가장 크게 드러난다.
- **반증**: 끈 쪽 격차가 같거나 더 크다(±5점 밖으로 커짐).
- **이 비교는 두 arm 각각의 run 안에서 만들어진 양끼리의 비교**라 세션 효과가 대부분 상쇄된다.

**기제 확인(가설 아님, 관측)**: 10분 창별 메모리 사유 탈락 비중. EXP-91이 한 시간 m1에서
켬 7~20% 대 끔 ≈0%를 쟀다. **여기서도 같은 갈림이 나오는지, 그리고 s1에서 가장 큰지**를 본다.

### 9.3 유효성 검사

| | 무엇 | 문턱 |
|---|---|---|
| V7 | **플래그가 실제로 먹었는지** — chat 없는 (인스턴스, 시각) 비율 | EXP-91 기준 켬 27~33%, 끔 0.1~1.0%. 끈 run이 **2% 미만**이어야 한다 |
| V8 | 도착 수가 §8의 네 run과 1% 이내 | `agent != "job_summary"`로 센다 |
| V9 | 구간별 실현 chat 비중이 목표와 0.5%p 이내 | 도착 시각이 같으므로 §8과 같은 값이 나와야 한다 |
| V10 | ⚠ **세션이 다르다** — §8의 `fspfx`는 2026-08-23 13:40~18:33, 이것은 같은 날 23:30 이후 | 총계 비교(H4)에 단서를 단다. H5는 run 안 양끼리라 영향이 작다 |

### 9.4 실행

```bash
ARMS=fsnoaff SESSION_PREFIX_BASE=exp93n  ./run_exp93_shift.sh shift 1
ARMS=fsnoaff SESSION_PREFIX_BASE=exp93nb ./run_exp93_shift.sh shift 1
```
연쇄 `/home/nxclab/tools/exp93n.sh`. 드라이버는 §7과 같은 스냅샷이고 `fsnoaff` arm이 이미
있다(`FS_PREFIX=true`를 명시로 박아 두어 `fspfx`와 선호 하나만 다르다). 채점은 같은 스크립트.

### 9.5 결과 (2026-08-24 01:46 KST, 2/2조건). **H4·H5 둘 다 반증. 대신 기제가 측정됐다**

표 전문 `results/aggregate_analysis/exp93/report_affinity.md`.

**유효성**: 기동 줄이 `affinity=false, affweight=0.00`이고 `prefix=true`를 포함한 나머지
다섯 플래그가 `fspfx`와 동일하다(V7 통과, 드라이버 검증도 통과). 도착 수 arm 간 0.02% 이내.

#### H4 반증 — 선호를 끄면 오히려 조금 나빠진다

| | 총계 offered | 반복 | chat | deepresearch | swe |
|---|---|---|---|---|---|
| 선호 **켬** | **75.5** | 75.3~75.7 | **87.0** | 56.3 | **48.8** |
| 선호 **끔** | **74.1** | 73.9~74.2 | **84.3** | 56.2 | **50.7** |
| 차이(끔−켬) | **−1.4** | | −2.7 | −0.1 | **+1.9** |

**사전 예측("이 trace는 교차점 위에 오래 있으니 선호가 3점 이상 해로울 것")은 부호부터
틀렸다.** 반복 범위가 겹치지 않으므로 1.4점은 재현되는 차이이지만 **사전에 정한 구별 문턱
3점 아래이고 예측과 반대 방향이다. 어느 쪽으로 읽어도 H4는 반증이다.**

> **따라서 정적 m1 45 req/s의 −7.1로 세웠던 "선호의 부호는 함대가 교차점 위에서 보내는 시간이
> 정한다"는 설명을 철회한다.** 이 trace는 균등 구간이 100%, 마지막 구간이 88% 무릎 위인데도
> 부호가 안 뒤집혔다. 남는 설명은 **그 −7.1이 정적 포화에 고유한 것**이라는 쪽이다 — 고정
> 도착률이 유지하는 정상상태 KV 압력을 움직이는 부하는 만들지 않는다. **B7에서 정적과 동적이
> 반대 방향을 가리킨 것과 같은 계열이고, 아직 측정으로 확인되지 않았다.**

그리고 **네 번째 축에서도 재분배다** — chat +2.7, swe −1.9. EXP-92의 균등 정적(chat +6.9,
총계 +0.6), EXP-91의 한 시간 m1(swe −3.0)과 같은 모양이다.

#### H5 반증 — 믹스 이동의 대가는 클래스 집중 때문이 아니다

§8.1의 구간 간 격차(chat 93% 구간 대 균등 구간, 짝지은 도착률 여섯 bin):

| | 10~15 | 15~20 | 20~25 | 25~30 | 30~35 | 35~40 | **평균** |
|---|---|---|---|---|---|---|---|
| 선호 켬 | +17.0 | +24.1 | +30.7 | +40.0 | +48.3 | +47.6 | **+34.6** |
| 선호 끔 | +17.8 | +25.0 | +31.4 | +40.3 | +50.2 | +46.6 | **+35.2** |

**차이 +0.6이고 방향이 예측과 반대다(끈 쪽이 근소하게 크다). 문턱 5점의 8분의 1 → 반증.**
**믹스 이동의 34~35점은 라우터가 클래스를 어떻게 묶느냐와 무관하다** — 요청 하나의 무게가
974에서 3,847 입력 토큰으로 무거워진 것이 원인이고, 배치 규칙으로 되돌릴 수 있는 것이 아니다.

#### 왜 효과가 없는가 — 두 제약이 같은 양을 두 번 읽는다

**이것이 이 조건이 실제로 산 것이다.** 인스턴스가 목적지로서 탈락한 사유의 구성(반복 2회):

| | pace 게이트 | incumbents | **메모리(KV)** | **앞의 둘 합** |
|---|---|---|---|---|
| 선호 **끔** | 71.1% (71.0/71.2) | 19.9% | **9.0%** (9.4/8.6) | **91.0%** |
| 선호 **켬** | 67.4% (67.8/67.1) | 17.5% | **15.1%** (14.7/15.4) | **84.9%** |
| 차이 | −3.7 | −2.4 | **+6.1** | **−6.1** |

**pace 쪽에서 줄어든 6.1점이 메모리 쪽에서 늘어난 6.1점과 같다.** 반복 간 폭이 0.4~0.8점이라
재현된다.

**기제**: 선호를 켜면 한 클래스가 적은 인스턴스에 모인다. ⑴ 그 결과 **chat이 빠진 인스턴스**가
생기고 그 인스턴스의 허용 속도가 chat 예산 50 ms에서 100 ms로 풀리므로 **게이트 탈락이
내려간다.** ⑵ 그런데 그 chat은 사라진 것이 아니라 **다른 인스턴스로 가서 거기에 토큰을 더
쌓으므로 메모리 탈락이 올라간다.**

> **두 판정 조건이 같은 양 — 인스턴스가 지금 들고 있는 토큰 수 — 을 다르게 읽은 것이다.**
> 토큰을 많이 들면 **토큰 하나당 시간이 길어지고(pace)** 동시에 **KV에 남는 자리가
> 줄어든다(메모리)**. **함대가 들어야 하는 총 토큰 수는 워크로드가 정하므로 배치로 바꿀 수
> 없고, 따라서 한쪽을 풀어 주면 반드시 다른 쪽을 조인다. 클래스 선호는 구조적으로 재분배
> 수단이고 용량을 만들 수 없다.**

**완전한 zero-sum은 아니고 작은 잔차가 선호 쪽에 남는다**: route가 76,058 대 74,602로 **1.9%
많고** shed가 18,598 대 18,928로 **1.7% 적다.** 그것이 총계 +1.4점의 정체다.

#### 대조 — 같은 라우팅 계층 안에서 무엇이 용량을 만드는가

EXP-78에서 거절을 전부 끄고 목적지 고르는 규칙만 다르게 했을 때 35 req/s에서 우리 라우팅이
**74.9점, vLLM router가 6.2점**이었다. 그 규칙은 **인스턴스를 남은 예산으로 값매겨 놓으면
실패할 자리에 안 놓는 것**이고, **성공하는 배치의 개수**를 바꾼다.

> **"라우팅"이라는 한 단어 아래 두 가지가 있었고 이번에 갈렸다. 인스턴스를 남은 예산으로
> 값매기는 것은 용량을 만들고, 클래스로 묶는 것은 수령인만 바꾼다.**

#### 논문에 쓸 문장

> "클래스를 묶는 것은 인스턴스 하나를 놓고 다투는 두 판정 조건 — 토큰당 속도 예산과 KV 한계 —
> 중 어느 쪽이 먼저 걸리는지를 바꾼다. 둘 다 같은 인스턴스 점유량을 읽으므로 한쪽에서 줄어든
> 탈락이 다른 쪽에서 거의 같은 크기로 늘어나고(−6.1점 대 +6.1점, 반복 2회), 배치되는 요청
> 수는 2% 안에서 같다. 따라서 이것은 클래스 사이의 배분 손잡이이지 용량을 만드는 기제가
> 아니다. 용량을 만드는 것은 인스턴스를 남은 예산으로 값매기는 쪽이다."

⚠ **쓰면 안 되는 것**: "선호는 아무것도 하지 않는다" — 함대 배치를 극단적으로 바꾸고
(EXP-91에서 chat 없는 인스턴스-시간 27~33% 대 0.1~1.0%) 클래스 사이에서 2~7점을 옮긴다.
안 바뀌는 것은 **총계**다.
