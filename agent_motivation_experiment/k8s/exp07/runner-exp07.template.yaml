# EXP-07 runner job template. Placeholders (sed-substituted by run_exp07.sh):
#   __JOBNAME__   e.g. bench-runner-exp07-r3600-t045
#   __RPM__       single rate condition in rpm (3600 = 60 req/s)
#   __SESSION__   e.g. exp07_kvadm_th045  (runner appends _rpm___RPM__)
# Everything else mirrors the exp05 runner (same workload/warmup/duration).
apiVersion: batch/v1
kind: Job
metadata:
  name: __JOBNAME__
  namespace: llumnix
spec:
  backoffLimit: 0
  completions: 1
  template:
    metadata:
      labels:
        app: bench-runner
    spec:
      serviceAccountName: bench-runner
      restartPolicy: Never
      containers:
        - name: runner
          image: llumnix-registry.cn-beijing.cr.aliyuncs.com/llumnix/vllm:20260306-165123
          workingDir: /work
          env:
            - { name: NVIDIA_VISIBLE_DEVICES, value: "void" }
            - { name: HF_HOME, value: /hf-cache }
            - { name: HF_HUB_OFFLINE, value: "1" }
            - { name: HF_DATASETS_OFFLINE, value: "1" }
            - { name: WORKLOAD, value: "sharegpt_request_level_poisson" }
            - { name: MODE, value: "rate-sweep" }
            - { name: RATE_LIST, value: "__RPM__" }
            - { name: LOAD_PROCS, value: "8" }
            - { name: DURATION_MIN, value: "5" }
            - { name: NUM_CONV, value: "1000" }
            - { name: SESSION, value: "__SESSION__" }
            - { name: RESTART_PER_CONDITION, value: "1" }
            - { name: EXTRA_ARGS, value: "--model meta-llama/Meta-Llama-3.1-70B-Instruct --load-threads 1024 --warmup-rpm 1200 --warmup-sec 60" }
            - { name: RESULT_UID, value: "3092" }
            - { name: RESULT_GID, value: "1999" }
          command: ["/bin/bash", "-lc"]
          args:
            - |
              set -uo pipefail
              ln -sf /usr/local/bin/k3s /usr/local/bin/kubectl
              kubectl version --client >/dev/null 2>&1 && echo "[runner] kubectl ok"
              PYBIN=/work/.venv/bin/python
              "$PYBIN" -c "import langchain_openai, datasets, tiktoken" \
                && echo "[runner] venv deps ok" || { echo "[runner] venv missing deps"; exit 1; }
              RESTART_FLAG=""
              [ "${RESTART_PER_CONDITION}" = "1" ] && RESTART_FLAG="--restart-per-condition"
              echo "[runner] starting: workload=${WORKLOAD} rate=${RATE_LIST}rpm session=${SESSION}"
              "$PYBIN" run_experiment.py \
                --engine llumnix --in-cluster \
                --workload "${WORKLOAD}" \
                --mode "${MODE}" \
                --rate-list "${RATE_LIST}" \
                --load-procs "${LOAD_PROCS}" \
                --duration-min "${DURATION_MIN}" \
                --workload-config "{\"num_conversations\": ${NUM_CONV}}" \
                --output-dir /work/results \
                --session-name "${SESSION}" \
                --metrics-interval 1.0 \
                ${RESTART_FLAG} ${EXTRA_ARGS}
              rc=$?
              chown -R "${RESULT_UID}:${RESULT_GID}" /work/results 2>/dev/null || true
              echo "[runner] done rc=$rc"
              exit $rc
          volumeMounts:
            - { name: work, mountPath: /work }
            - { name: k3s, mountPath: /usr/local/bin/k3s, readOnly: true }
            - { name: hf-cache, mountPath: /hf-cache }
      volumes:
        - name: work
          hostPath:
            path: /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment
            type: Directory
        - name: k3s
          hostPath:
            path: /usr/local/bin/k3s
            type: File
        - name: hf-cache
          hostPath:
            path: /NHNHOME/huggingface
            type: Directory
