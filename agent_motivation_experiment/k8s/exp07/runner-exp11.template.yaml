---
# EXP-11: chat deep-overload extension (exp05 continuation on the current
# 3.1-70B stack). sharegpt_request_level_poisson at rates past raw capacity
# (~100 req/s) so the queue-bound ITL regime observed in EXP-10 becomes
# visible for the short-prompt workload. 24 procs x 2048 threads (=49k
# in-flight capacity) keeps the stream open-loop up to ~175 req/s x 10min.
# Requires the no-timeout gateway (patch-gateway-timeout.sh) + 64Gi.
# Placeholders: __JOBNAME__ __SESSION__ __RATES__ __DURMIN__
apiVersion: batch/v1
kind: Job
metadata:
  name: __JOBNAME__
  namespace: llumnix
spec:
  backoffLimit: 0
  completions: 1
  template:
    metadata:
      labels:
        app: bench-runner-exp11
    spec:
      serviceAccountName: bench-runner
      restartPolicy: Never
      containers:
        - name: runner
          image: llumnix-registry.cn-beijing.cr.aliyuncs.com/llumnix/vllm:20260306-165123
          workingDir: /work
          env:
            - { name: NVIDIA_VISIBLE_DEVICES, value: "void" }
            - { name: HF_HOME, value: /hf-cache }
            - { name: HF_HUB_OFFLINE, value: "1" }
            - { name: HF_DATASETS_OFFLINE, value: "1" }
            - { name: RATE_LIST, value: "__RATES__" }
            - { name: DURATION_MIN, value: "__DURMIN__" }
            - { name: LOAD_PROCS, value: "24" }
            - { name: RESULT_UID, value: "3092" }
            - { name: RESULT_GID, value: "1999" }
          command: ["/bin/bash", "-lc"]
          args:
            - |
              set -uo pipefail
              ln -sf /usr/local/bin/k3s /usr/local/bin/kubectl
              kubectl version --client >/dev/null 2>&1 && echo "[runner] kubectl ok"
              PYBIN=/work/.venv/bin/python
              "$PYBIN" -c "import langchain_openai, datasets, tiktoken" \
                && echo "[runner] venv deps ok" || { echo "[runner] venv missing deps"; exit 1; }
              echo "[runner] exp11 chat deep sweep: rates=${RATE_LIST} rpm dur=${DURATION_MIN}min"
              "$PYBIN" run_experiment.py \
                --engine llumnix --in-cluster \
                --workload sharegpt_request_level_poisson \
                --mode rate-sweep \
                --rate-list "${RATE_LIST}" \
                --duration-min "${DURATION_MIN}" \
                --load-procs "${LOAD_PROCS}" \
                --load-threads 2048 \
                --restart-per-condition \
                --workload-config '{"num_conversations": 1000}' \
                --model meta-llama/Meta-Llama-3.1-70B-Instruct \
                --warmup-rpm 1200 --warmup-sec 60 \
                --post-duration-grace 60 \
                --output-dir /work/results \
                --session-name "__SESSION__" \
                --metrics-interval 1.0
              rc=$?
              chown -R "${RESULT_UID}:${RESULT_GID}" /work/results 2>/dev/null || true
              echo "[runner] done rc=$rc"
              exit $rc
          volumeMounts:
            - { name: work, mountPath: /work }
            - { name: k3s, mountPath: /usr/local/bin/k3s, readOnly: true }
            - { name: hf-cache, mountPath: /hf-cache }
      volumes:
        - name: work
          hostPath:
            path: /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment
            type: Directory
        - name: k3s
          hostPath:
            path: /usr/local/bin/k3s
            type: File
        - name: hf-cache
          hostPath:
            path: /NHNHOME/huggingface
            type: Directory
