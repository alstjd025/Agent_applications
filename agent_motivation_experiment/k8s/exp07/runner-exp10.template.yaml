---
# EXP-10 E2: open-loop request-level replay lambda sweep.
# codingagent_request_level_poisson replays the E1 transcript verbatim as
# independent Poisson arrivals (lambda = requests/sec), admission OFF,
# cold restart per condition, all client-side aborts disabled so every
# request is measured rather than killed (absolute-SLO analysis).
# Placeholders: __JOBNAME__ __SESSION__ __LAMBDAS__ __DURMIN__
apiVersion: batch/v1
kind: Job
metadata:
  name: __JOBNAME__
  namespace: llumnix
spec:
  backoffLimit: 0
  completions: 1
  template:
    metadata:
      labels:
        app: bench-runner-exp10
    spec:
      serviceAccountName: bench-runner
      restartPolicy: Never
      containers:
        - name: runner
          image: llumnix-registry.cn-beijing.cr.aliyuncs.com/llumnix/vllm:20260306-165123
          workingDir: /work
          env:
            - { name: NVIDIA_VISIBLE_DEVICES, value: "void" }
            - { name: HF_HOME, value: /hf-cache }
            - { name: HF_HUB_OFFLINE, value: "1" }
            - { name: HF_DATASETS_OFFLINE, value: "1" }
            - { name: TRANSCRIPT, value: "/work/results/exp10_transcript/transcript_swe_calls.jsonl" }
            - { name: LAMBDA_LIST, value: "__LAMBDAS__" }
            - { name: DURATION_MIN, value: "__DURMIN__" }
            - { name: LOAD_PROCS, value: "8" }
            - { name: RESULT_UID, value: "3092" }
            - { name: RESULT_GID, value: "1999" }
          command: ["/bin/bash", "-lc"]
          args:
            - |
              set -uo pipefail
              ln -sf /usr/local/bin/k3s /usr/local/bin/kubectl
              kubectl version --client >/dev/null 2>&1 && echo "[runner] kubectl ok"
              PYBIN=/work/.venv/bin/python
              "$PYBIN" -c "import langchain_openai, datasets, tiktoken" \
                && echo "[runner] venv deps ok" || { echo "[runner] venv missing deps"; exit 1; }
              [ -s "${TRANSCRIPT}" ] || { echo "[runner] transcript missing: ${TRANSCRIPT}"; exit 1; }
              echo "[runner] exp10 replay sweep: lambdas=${LAMBDA_LIST} dur=${DURATION_MIN}min"
              "$PYBIN" run_experiment.py \
                --engine llumnix --in-cluster \
                --workload codingagent_request_level_poisson \
                --mode poisson-sweep \
                --lambda-list "${LAMBDA_LIST}" \
                --duration-min "${DURATION_MIN}" \
                --load-procs "${LOAD_PROCS}" \
                --load-threads 1024 \
                --restart-per-condition \
                --transcript-file "${TRANSCRIPT}" \
                --disable-timeouts \
                --model meta-llama/Meta-Llama-3.1-70B-Instruct \
                --warmup-rpm 60 --warmup-sec 60 \
                --output-dir /work/results \
                --session-name "__SESSION__" \
                --metrics-interval 1.0
              rc=$?
              chown -R "${RESULT_UID}:${RESULT_GID}" /work/results 2>/dev/null || true
              echo "[runner] done rc=$rc"
              exit $rc
          volumeMounts:
            - { name: work, mountPath: /work }
            - { name: k3s, mountPath: /usr/local/bin/k3s, readOnly: true }
            - { name: hf-cache, mountPath: /hf-cache }
      volumes:
        - name: work
          hostPath:
            path: /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment
            type: Directory
        - name: k3s
          hostPath:
            path: /usr/local/bin/k3s
            type: File
        - name: hf-cache
          hostPath:
            path: /NHNHOME/huggingface
            type: Directory
